# 审稿意见处理状态（2026-09-02）

状态说明：`已修改` 表示正文、图或实验已经落地；`已降级表述` 表示缺少原始证据，正文已删除或限制相应主张；`需作者材料` 表示不能在不虚构数据的前提下补齐；`无线负责人` 表示按当前分工不新增无线/ns-3 实验。

## Reviewer 1

| 意见 | 状态 | 已采取的修改 |
|---|---|---|
| Cross-domain 含义不清，三个数据集来自独立 campaign | 已修改 | 摘要、引言、实践数据集章节和结论均明确：共同点是发布、元数据、表示和构造任务；不声称三个 campaign 同步，也不推断物理因果。 |
| LLM 模型、调用、失败率、成本、延迟、人工干预；增加脚本基线 | 部分已修改；需作者材料 | 增加确定性枚举器基线和 6,144-key 集合核验；正文逐项列出未归档的 LLM 信息，并把结论限制为 feasibility demonstration。模型/API 原始日志只能由作者补充。 |
| 增加数据集汇总表；考虑 Croissant | 已修改 | 新增数据集表；增加 `croissant_metadata.json`，包含 5 个关键文件的字段映射、大小和 SHA-256。最终投稿前需把该文件提交到公开仓库并确认 licence。 |

## Reviewer 2

| 编号 | 状态 | 已采取的修改 |
|---|---|---|
| 1. 引言贡献层级不清 | 已修改 | 按“实践数据 / 生成数据”两条主线重写贡献，并区分 telemetry、semantic abstraction 与 LLM-driven DT 的层级。 |
| 2. Related Work 结构重复、衔接不清 | 已修改 | 三节分别聚焦数据可用性与 telemetry、隐私感知语义抽象、DT/LLM 生成数据；加入从实践 telemetry 到 synthetic complement 的过渡，并删除重复论述。 |
| 3. 现有工作局限重复 | 已修改 | 跨域异构与对齐局限集中在 II-A；II-C 不再重复展开，而聚焦生成数据的必要性和人工配置成本。 |
| 4. Related Work 与 Introduction 重叠 | 已修改 | 引言保留问题与贡献，Related Work 改为“既有证据—局限—本文定位”的结构。 |
| 5. Orchestrator 如何协调 Fig. 1 中组件 | 已修改 | 明确 Fig. 1 是 scope map；本文 orchestrator 只调度被演示的数据生成序列，不声称控制所有 AI engine/controller。 |
| 6. II-C 的 autonomous closed-loop 说法缺引文 | 已修改 | 增加相关 DT assistant/agent 工作引文，并把论断限定为这些工作仍需要显式目标与配置范围。 |
| 7. Scalability 未评估 | 已修改 | 删除摘要和架构中的 scalability 主张；Kafka 只作为实现组件描述，不作为贡献或性能证据。 |
| 8. IV-A 冗长、继承关系和 prospective 功能不清 | 已修改 | 压缩 InfluxDB/PostgreSQL/Kafka 通用描述，标明继承自既有工作，明确 forecasting、controller、local AI 等未评估。 |
| 9. V1 八个语义特征缺乏细节 | 已修改 | 补充原始特征来源、八维公式/解释、选择理由、任务标签、模型结构和训练协议。 |
| 10. V3 为什么称 privacy-aware | 已修改 | 加入 raw/PCA/random/majority 基线、四类 attacker 和十种子区间；只称 privacy-aware，不称 privacy-preserving，并陈述 PCA 的不同权衡。 |
| 11. 无线信号是否由光链路转换 | 已修改 | 明确无线数据完全来自独立无线接入测试台，不由光链路转换，也未与光学 campaign 同步。 |
| 12. Orchestrator 位置与箭头语义不清 | 已修改 | 正文说明 orchestrator 接收请求并依次调用全部五个角色；下方箭头是共享 orchestration layer，右下布局不是执行顺序或局部控制范围。 |
| 13. Fig. 1 与 Fig. 6 重复；AI engine/controller 无结果 | 已修改 | 图注和正文区分 scope map 与 evaluated sequence；AI engine/controller 被明确标为 contextual/prospective，删除未验证的闭环能力主张。 |
| 14. 五个组件是否真是 agents、为何需要、开销 | 已修改/已降级表述 | 定义为 orchestrated LLM-invocation roles 而非持久自治体；说明工具、共享状态、模块化理由和可替换为确定性函数；明确未量化通信开销。 |
| 15. Reflection Agent 文字对比度不足 | 已修改 | 从 PPT 源文件把标题和说明改为深色，导出 4621×2209 的 `architecture_LLM_revision.png`。 |
| 16. Fig. 7 分辨率不足 | 已修改 | 用去重 CSV 重绘 5846×2526 图片，并修正旧图中 8,160、6,144、6,120、24 的口径混淆。 |

## Reviewer 3

| 编号 | 状态 | 已采取的修改 |
|---|---|---|
| 1. 语义融合实验不可复现、随机划分泄漏 | 已修改 | 识别具体光/无线文件，给出样本、标签、confidence、结构和训练参数；使用 chronological split、purge gap、train-only preprocessing 和十随机种子。 |
| 2. 原 Table 1 不能证明隐私提升 | 已修改 | 重做 E1/E2；加入 raw、PCA、随机投影、多数类/随机基线和 LR/RF/MLP/GB 四类攻击器，报告均值与 95% CI，结论改为 trade-off。 |
| 3. LLM 框架没有评估 | 已修改/已降级表述 | 增加 deterministic enumeration baseline；缺失模型、prompt、tokens/cost 等归档信息，正文明确“not reported”，不再声称效率优势。 |
| 4. 生成数据缺少独立正确性验证 | 已修改；需作者材料 | 扫描全部结果 warning；对 96 个分层样本在 GNPy 2.13/2.14 复现，发现公开 equipment schema 无法加载；报告 commit 和混杂因素。要验证数值仍需原始工作环境或修正配置。 |
| 5. 摘要区分 implemented/evaluated/demonstrated/proposed | 已修改 | 摘要明确 practical collection、constructed semantic benchmark、GNPy artifact audit，并限制 LLM claim。 |
| 6. Sections III 与 IV-A 重叠 | 已修改 | Section III 缩为范围与边界，组件实现细节集中到 IV-A。 |
| 7. AC/DC 供电细节 | 已修改 | 已删除。 |
| 8. $-5$ dBm optimal claim | 已修改 | 改为该 campaign 采用的 operating point，明确不声称最优。 |
| 9. 英美拼写混用 | 已修改 | 活跃正文统一为英式拼写；旧美式拼写只存在于注释块，不进入稿件。 |
| 10. ns-3 与标题大小写 | 已修改 | 正文统一使用 `ns-3`，主要标题采用一致的 title case。 |
| 11. 首页 placeholder DOI | 已修改 | DOI 模板保持注释，不出现在编译稿。 |
| 12. 三处拼写/语法错误 | 已修改 | `includign BER`、`based over`、`formats,i.e.` 均已修正或对应句子已重写。 |

## 仍不能由当前材料完成的事项

1. 原始 LLM model/version、完整 prompts、tool schema、每次调用与重试、tokens、成本、延迟、失败率和人工干预记录。
2. 能加载公开 equipment 文件并复现数值的原始 GNPy commit/容器，或由作者确认修正 equipment 后重新生成数据。
3. 新的无线/ns-3 实验及无线模型真实性验证，按当前分工交由无线负责人。
4. 最终公开仓库的正式 licence/SPDX 选择，以及把 Croissant 文件与修订脚本同步到公开 release。
