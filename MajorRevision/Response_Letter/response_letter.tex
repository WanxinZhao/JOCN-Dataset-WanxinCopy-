\documentclass[11pt]{article}
\usepackage[a4paper,margin=2.25cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{array}
\usepackage{microtype}
\hypersetup{colorlinks=true,linkcolor=blue!55!black,urlcolor=blue!55!black}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\definecolor{reviewblue}{RGB}{31,78,121}
\definecolor{responsegreen}{RGB}{32,112,74}
\definecolor{lightblue}{RGB}{239,246,252}
\newcommand{\manutitle}{Open-Source Cross-Domain Datasets Enabled by LLM-Driven Digital Twins and Network Telemetry}
\newenvironment{reviewercomment}{\begin{quote}\color{reviewblue}}{\end{quote}}
\newenvironment{authorresponse}{\color{responsegreen}\textbf{Response.} }{\par\color{black}}
\newcommand{\change}[1]{\par\textbf{Change in the manuscript.} #1\par}
\newcommand{\evidence}[1]{\par\textbf{Evidence and boundary.} #1\par}

\begin{document}

\begin{center}
{\Large\bfseries Response to the Reviewers}\par
\vspace{0.4em}
{\large\itshape \manutitle}\par
\vspace{0.6em}
Sen Shen, Wanxin Zhao, Xuewen Chen, et al.
\end{center}

\hrule
\vspace{0.8em}

We thank the reviewers for their careful and constructive assessment. We reorganised the article around practical and generated datasets, clarified the contribution of the cross-domain architecture, expanded the experimental methodology, and added reproducibility and correctness checks. Detailed execution records and file-level metadata are provided in the public revision artefacts.

The principal revisions are:
\begin{enumerate}[leftmargin=1.6em]
  \item We clarify that cross-domain describes the implemented system architecture and unified data-interface workflow, while platform scheduling required the practical datasets to be collected in different time periods.
  \item We apply CC BY-NC 4.0 to the released datasets and associated metadata/documentation, use ``openly available'' consistently, and state the licence in Table~2 and Croissant metadata.
  \item We define the multi-agent workflow operationally through schema-defined roles, shared orchestrator state, simulator tools, and retained intermediate artefacts, and evaluate it through an instrumented run and a deterministic baseline.
  \item We add leakage-free temporal semantic evaluation, multiple privacy attackers and baselines, a deterministic GNPy enumerator, versioned GNPy replay, and bounded ns-3 validation.
  \item We remove the template DOI and compilation date, rebuild all citations and references, enlarge Fig.~6, regenerate Fig.~7, and standardise terminology and British English.
\end{enumerate}

Text shown in green describes our response; reviewer comments are shown in blue. Repository paths are retained where they are the stable names of released artefacts.

\section*{Reviewer 1}

\begin{reviewercomment}
The paper addresses a relevant problem and presents a good collection of datasets and tools

A few comments to improve the quality

\textbullet{} The ``cross-domain'' claim is not really clear, the optical, sensing, and wireless datasets seem to originate from separate measurement campaigns. Please clarify
\end{reviewercomment}
\begin{authorresponse}
We thank the reviewer for this comment. Cross-domain refers to the framework architecture and the dataset-level workflow. Optical-transport, fibre-sensing, and wireless-access sources are organised with common publication metadata. For the evaluated optical--wireless benchmark, a unified data interface loads the separately acquired optical and wireless records, applies domain-specific preprocessing, and supplies their features to the learning workflow; the semantic modules process exported wireless records. Experimental-platform scheduling required the practical campaigns to be collected in different periods, and Section~IV-B specifies how the benchmark pairs their records.
\end{authorresponse}
\change{The Abstract, Section~III, Section~IV-B, the practical dataset description, and the Conclusions now distinguish the cross-domain system architecture from the different acquisition periods and describe the unified data-interface calls used during training.}

\begin{reviewercomment}
\textbullet{} Provide details about the LLM (model used, number of attempts, tool interface, failure rate, cost, latency, or degree of human intervention). Would it be feasible a conventional scripting pipeline as baseline?
\end{reviewercomment}
\begin{authorresponse}
We now position the LLM workflow as a feasibility demonstration. A separately labelled instrumented run used \texttt{gpt-4o-mini-2024-07-18}, temperature 0.2, a 90-s timeout, at most three attempts per call, and strict mode without silent rule fallback. Planner, Scenario Expansion, and Reflection were each called twice. All six API calls succeeded on their first attempt, consuming 106,041 input and 2,267 output tokens (108,308 total), with 26.22~s summed API latency and an estimated text-token cost of US\$0.01727 using the dated price snapshot stored with the run.

Manual recovery was required during simulator execution. An earlier invocation made two successful API calls (4,057 tokens; estimated US\$0.0009339) before it was stopped after 344 GNPy import failures and one all-off result. Recovery included installing the local package, correcting inactive-channel representation and result extraction, repairing a single-channel case, resuming from manifest-hashed files, correcting the recovery log callback, and rerunning nine failed scenarios. A later deterministic replay corrected a power-sweep override and reused three saved LLM responses, so it incurred no new API calls. These events are reported here and in the public trace; the article states only that manual recovery was required.

The independent script enumerates the expected Cartesian product of four destinations, two modulation/slot-width settings, 256 occupancy patterns, and three powers. It generates 6,144 unique configuration keys without an LLM or simulator execution. The LLM-orchestrated history contains 8,192 execution records but the same 6,144 unique keys, hence a 25\% repetition rate and set precision/recall of 1.0. We do not compare enumeration-only time with end-to-end LLM plus GNPy runtime and make no superiority claim.
\end{authorresponse}
\change{Section~V-B and Tables~3--4 report the model/settings, observed calls, usage, cost, API latency, manual-recovery boundary, and deterministic baseline. Detailed chronology, hashes, requests, responses, and failure logs remain in the revision repository.}
\evidence{The trace contains six rendered requests, six complete responses, per-call records, prompts, model identifiers, timings, token usage, source/input hashes, and recovery history. This new run does not reconstruct the unavailable historical API trace and is not a repeated-trial reliability estimate.}

\begin{reviewercomment}
\textbullet{} Would be nice to have a summary table for each dataset reporting the number of samples, features, labels, file format, storage size, licence... You may also consider to align with \url{https://mlcommons.org/working-groups/data/croissant/}
\end{reviewercomment}
\begin{authorresponse}
We added a dataset summary table and a Croissant~1.0-compatible JSON-LD record. The table distinguishes the two 339-km Voyager campaigns, the 986-km and urban-sensing sources, wireless telemetry, the constructed benchmark, and both synthetic datasets. It reports record/sampling information, principal fields or labels, formats and available processed sizes, and access/reuse terms. The synthetic wireless entry identifies ns-3, 48 configurations, 432 application-flow records, 624 raw FlowMonitor rows, and the LTE/EPC, Wi-Fi 802.11n, and CSMA-based LiFi-surrogate branches.

We cross-checked the practical-data README, Croissant record, Table~2, and manuscript text. The 2023--24 Voyager product contains 1,373 processed 15-minute records. The distinct 2025--26 campaign contains 20,960 valid eight-channel records with real timestamps, mostly 70--72-s spacing and acquisition gaps, and was not resampled to 15-minute intervals.

We also resolved the licence terminology directly. The authors intend the datasets to be reusable for non-commercial research, so we added a repository-level data-licence notice applying Creative Commons Attribution--NonCommercial 4.0 International (CC BY-NC 4.0) to the released datasets and associated metadata/documentation. This licence permits sharing and adaptation with attribution for non-commercial purposes. Software and identified third-party material retain their own terms. Because the non-commercial condition is narrower than the formal open-source definition, the manuscript uses ``openly available'' rather than the potentially ambiguous ``open-source'' label.
\end{authorresponse}
\change{Table~2 and the practical dataset subsection now identify CC BY-NC 4.0 explicitly. The Abstract, Introduction, and Conclusions use ``openly available'', and the Croissant \texttt{license} field contains the canonical licence URL. The original manuscript title is retained.}
\evidence{The repository contains a root \texttt{LICENSE} notice; the practical-data release package links to the same terms. The Croissant record includes the licence, access locations, sizes, checksums, and field units where defined. Unknown external collection totals and undocumented raw wireless units are marked unavailable rather than inferred.}

\section*{Reviewer 2}

\begin{reviewercomment}
This paper presents an open-source framework for constructing a dataset that integrates the optical- and wireless-communication domains. The dataset comprises both real-world and synthetic data. Overall, the paper presents substantial work and covers a broad range of topics. However, the paper does not always distinguish the primary contributions from supporting material, some descriptions are unclear, and the boundary between this study and the authors' previous work is insufficiently defined. The authors are requested to address the following comments and revise the manuscript accordingly.

1. The hierarchy of the contributions presented in the Introduction is unclear

The authors state that ``this paper presents an open-source cross-domain dataset framework enabled by network telemetry, semantic data abstraction, and LLM-driven multi-agent DTs.'' However, these three elements are not fully parallel. The first two relate to practical datasets, whereas LLM-driven multi-agent DTs relate to generated datasets. The contribution could instead be stated as follows: ``This paper presents an open-source cross-domain dataset framework that supports both practical and generated datasets: the former through network telemetry and semantic data abstraction, and the latter through LLM-driven multi-agent DTs.''
\end{reviewercomment}
\begin{authorresponse}
We adopted this hierarchy. Practical datasets are organised through telemetry and common metadata, while wireless semantic abstraction is evaluated in the constructed optical--wireless benchmark. Generated datasets are demonstrated through the LLM-driven DT workflow. The four contributions distinguish inherited telemetry infrastructure from the new dataset organisation, semantic evaluation, DT demonstration, and reproducibility audit.
\end{authorresponse}
\change{The Abstract, final Introduction paragraphs, contribution list, and Conclusions were rewritten around practical versus generated data.}

\begin{reviewercomment}
2. The Related Work section is not clearly organized

Subsections A, B, and C contain repeated content and arguments, conflicting statements, an unclear progression of ideas, and insufficient transitions. Some specific issues are described below:

1. The writing style and focus are inconsistent across the three subsections. Subsection A objectively summarizes existing studies without directly explaining the specific improvements introduced in this paper. Subsection B describes how previous work motivates and supports this study, but mentions the proposed improvement only in its final sentence. Subsection C objectively evaluates existing studies and briefly introduces the work related to this paper. The authors are advised to adopt a consistent structure: objectively assess the existing literature, identify its limitations, explain how it motivates or supports the present study where applicable, and then briefly introduce the proposed approach.

2. The opening statements of Subsections A, B, and C are not fully consistent. Subsection A argues that existing datasets are inadequate and proposes supplementing real-world data through telemetry. Subsection B discusses semantic extraction and privacy protection for real-world data. However, Subsection C begins by stating: ``However, real network measurements are difficult to collect at scale because of limited testbed access, operational cost, privacy constraints, safety requirements, and the rarity of abnormal or extreme operating conditions. This challenge is more pronounced in cross-domain optical-wireless networks, where heterogeneous infrastructures, telemetry interfaces, and data formats complicate data collection and alignment.'' This appears potentially self-contradictory. If the proposed work uses telemetry and, to some extent, addresses heterogeneous alignment and semantic extraction for real-world data, the quoted statement should be reconciled with those contributions. If the present work does not address these issues, the extensive discussion of these limitations in Subsections A and B appears less relevant. In addition, the transition from Subsections A and B to Subsection C needs to be strengthened.

3. The discussion of limitations in existing work is repetitive. The lack of uniformity between optical and wireless infrastructures and data characteristics, as well as the resulting alignment challenges, are discussed repeatedly in Subsections A and C. It would be preferable to complete this discussion in Subsection A. Subsection C could then focus on the value and importance of generated data, the limitations of related studies, and the solution proposed in this paper, without reopening the same cross-domain issues from the perspective of generated data. In addition, all three subsections mention that existing studies primarily address a single domain rather than cross-domain settings, while both Subsections A and B discuss data-privacy concerns. Each subsection should have a clearly defined primary focus and supporting scope so that the Related Work section develops progressively and distinguishes the main points from secondary ones.

4. The Introduction already presents the background, significance, and research content of this study. The authors should avoid turning the Related Work section into a more detailed, topic-by-topic version of the Introduction. Instead, this section should focus on the limitations of existing studies relative to the present work, the ways in which prior studies motivate or support this work, and the advances offered by the proposed approach.
\end{reviewercomment}
\begin{authorresponse}
We reorganised the section into three functions: cross-domain data availability and telemetry; semantic abstraction and empirical privacy evaluation; and DT/LLM-generated data. Each subsection now describes representative work, identifies the remaining limitation, and closes with the corresponding advance in this study. Section~II-C begins with the distinct value of controllable synthetic data rather than reopening the telemetry-alignment discussion.

We consolidated data availability and telemetry in Section~II-A, information selection and privacy in Section~II-B, and controllable scenario generation in Section~II-C. The three subsections now have separate primary questions. We also shortened the Introduction to the problem, the two-part framework, and the contribution hierarchy.
\end{authorresponse}
\change{Section~II was rewritten; repeated cross-domain/privacy arguments were removed; and the Introduction was shortened so it no longer duplicates the literature review.}

\begin{reviewercomment}
5. The first sentence of the fourth paragraph in Section III states: ``The orchestrator coordinates the interactions among multiple agents, DTs, AI engines, data repositories, and network controllers.'' However, Fig. 1 appears to show only data-flow interactions between the orchestrator and the Agentic AI layer. How does the orchestrator coordinate or control the other components?
\end{reviewercomment}
\begin{authorresponse}
We rewrote Section~III to describe the implemented relationships directly. The orchestrator owns the shared task state and schedules planning, scenario expansion, DT execution, result analysis, optional reflection, and reporting; it exchanges structured requests and results with the role modules and invokes GNPy or ns-3 through simulator adapters. The AI engine and SDN controller are separate services inherited from our implemented optical telemetry platform~[\emph{JOCN}, 17(2), 139--150, 2025]. They consume repository data and prediction outputs, but the DT-workflow orchestrator does not dispatch those services.
\end{authorresponse}
\change{Section~III identifies the orchestrator's task path and simulator interface and explains how the generated-data workflow relates to the previously implemented telemetry, AI-engine, and controller services. The Fig.~1 caption distinguishes data exchange from task and control actions.}

\begin{reviewercomment}
6. Please add references to support the following statement in Section II-C: ``Existing DT and LLM-agent workflows generally assume that simulation objectives, scenario spaces, parameter ranges, and data-generation requirements are manually specified. Limited work has considered dataset generation itself as an autonomous closed-loop workflow, especially across optical and wireless domains.''
\end{reviewercomment}
\begin{authorresponse}
We replaced the unsupported generalisation with a comparison tied to cited DT assistants and AI-agent studies. Those works automate modelling or network operation but use supplied objectives or parameter ranges. We describe dataset generation as the workflow-design problem examined here and limit our own result to feasibility.
\end{authorresponse}
\change{Section~II-C now cites the relevant GPT/DT assistant, LLM--DT management, local-agent, and DT-aided control studies immediately beside the bounded comparison.}

\begin{reviewercomment}
7. The abstract states: ``The proposed framework integrates distributed and centralised data repositories, Kafka-based telemetry pipelines, DTs, AI engines, and network controllers to support scalable data collection, cross-domain data fusion, and automated dataset generation.'' How is scalability demonstrated or evaluated in the paper?
\end{reviewercomment}
\begin{authorresponse}
Scalability was not evaluated in this study, so we removed it from the Abstract and from the claimed contributions. Kafka remains in the architecture as an implemented telemetry transport component, without a scalability conclusion.
\end{authorresponse}
\change{The Abstract, framework scope, telemetry section, and Conclusions no longer claim demonstrated scalability.}

\begin{reviewercomment}
8. Section IV-A provides an extensive description of the data-acquisition system, but several issues should be addressed. First, the respective roles of InfluxDB and PostgreSQL are repeated in Section III and Sections IV-A.2 and IV-A.4; this implementation logic should not be described excessively. Second, this part of the system appears to be inherited from the authors' previous work, but the manuscript does not clearly distinguish the innovations of the present study from inherited components. If the entire acquisition system comes from previous work, the space devoted to it is disproportionate to that devoted to the cross-domain semantic modules introduced in this paper. Third, the manuscript includes excessive descriptions of the generic capabilities of third-party components. For example, a general introduction to Kafka is not an author contribution and should be shortened or removed. Finally, the authors should focus on functions that have actually been implemented rather than mixing implemented capabilities with future or potential ones. For example, have the following functions already been implemented: ``In addition, lightweight local AI/ML modules can be deployed on distributed databases to support local inference, such as short-term performance forecasting and anomaly detection'' in Section IV-A.2, and ``It also supports on-demand aggregation, where only task-relevant telemetry streams are forwarded for cross-domain analysis, optimisation, or orchestration'' in Section IV-A.3? The authors should review the manuscript and clearly distinguish implemented functions from prospective capabilities throughout.
\end{reviewercomment}
\begin{authorresponse}
We state the inheritance and additions at the beginning of Section~IV-A. The previous optical platform implemented acquisition, InfluxDB/PostgreSQL storage, Kafka transfer, OpenFaaS AI services, and SDN-controller interfaces. The cited field trial reported database-to-AI transfer below 0.05~s, multi-link ANN QoT inference below 0.3~s, and controller-driven Voyager reconfiguration. The present study adds wireless telemetry, semantic processing of exported records, the unified optical--wireless training interface, and the DT data-generation workflow. Section~IV-A retains brief, task-specific explanations of the two database roles, Telegraf collection, and Kafka topic transfer, while removing generic performance claims and unsupported local-inference statements.
\end{authorresponse}
\change{Section~IV-A now distinguishes the previously implemented platform from the additions in this study, gives concise evidence for the inherited AI/controller functions, and describes only the data path needed to understand the released datasets.}

\begin{reviewercomment}
9. Section IV-B requires more detail. In Section IV-B.1, the authors state that eight semantic features are used. From which raw features are they calculated, how are they calculated, and why were these particular features selected? Does this step correspond to the ``task-oriented semantic communication'' discussed in Section II-B? The authors are advised to provide a more detailed description of this part of the work.
\end{reviewercomment}
\begin{authorresponse}
We specify the 12 input features derived from 30-s wireless records, training-only activity thresholds and scalers, the four targets (load, access diversity, stability, and congestion risk), and the exact equations for diversity, stability, and risk. V1 transmits eight deterministic indicators covering traffic intensity, temporal stability, multi-access availability, signal quality, inactivity, and composite risk. V2/V3 encode the same task context through learned bottlenecks.
\end{authorresponse}
\change{Section~IV-B.1 now gives the formulas, architecture sizes, optimisation settings, confidence definition, and rationale.}
\evidence{The released material records exact seeds, preprocessing parameters, source fields, and per-seed outputs. The confidence value is a heuristic, not a calibrated probability.}

\begin{reviewercomment}
10. Why is V3 described as ``privacy-aware''? Does it introduce a substantive improvement over V2, or is the term used simply because three dimensions expose less information than eight dimensions and the added noise makes reconstruction more difficult?
\end{reviewercomment}
\begin{authorresponse}
We use privacy-aware to denote a design and evaluation objective, not a formal guarantee. V3 varies bottleneck dimension and training-time noise; attribute inference is evaluated with logistic regression, random forest, MLP, and histogram gradient boosting against raw, V1/V2, PCA, random-projection, majority, and random baselines. V3 moderately reduces observed CPE leakage relative to raw telemetry while maintaining V1-level binary utility, but PCA leaks less and also loses utility. V3 is not universally best.
\end{authorresponse}
\change{The semantic introduction, results, Fig.~4, Table~1, and Conclusions now use the bounded empirical interpretation.}

\begin{reviewercomment}
11. The sources of the optical-link signals are clearly described, whereas the wireless-signal source is identified only as ``a wireless access network.'' Are the wireless signals converted from signals on one of the optical links, as suggested by Figs. 1 and 6?
\end{reviewercomment}
\begin{authorresponse}
No. The wireless telemetry comes from the 5G-CLARITY/REASON multi-access testbed and is not converted from either optical link. The optical and wireless datasets enter the cross-domain workflow through the unified data interface during subsequent processing and training.
\end{authorresponse}
\change{The practical wireless subsection now identifies the wireless testbed and its 5G, Wi-Fi, and LiFi configuration explicitly.}

\begin{reviewercomment}
12. The overall multi-agent architecture appears to follow an orchestrator-executor pattern, yet the subagents (e.g., the planner and report agents) seem to operate in a fixed sequence. Is the role and placement of the orchestrator consistent with a conventional multi-agent architecture? Typically, an orchestrator receives the user request and then plans and dispatches tasks to subagents. Why is it positioned in the lower-right corner of the figure? The arrows beneath the first three agents appear to indicate direct communication with those agents. Does the orchestrator operate only for the final two agents, or do those arrows instead represent communication with the Agentic AI layer as a whole? The current diagram does not make this architecture clear.
\end{reviewercomment}
\begin{authorresponse}
The orchestrator owns the shared task state and schedules planning, scenario expansion, execution, analysis, reflection, and reporting. In Fig.~6, the upper row conveys the role sequence, while the lower blocks show the knowledge base, DT, and orchestrator. The arrows below the roles identify access to these different components; the orchestrator's lower-right position indicates its control-plane role rather than a final stage in the sequence. Section~V-A specifies its coordination of the complete workflow.
\end{authorresponse}
\change{The revised Fig.~6 distinguishes user input, role sequence, shared state, DT tool, and retained output. The article retains only the operational sequence; this layout-level explanation is provided here. The figure is now cropped to its content and displayed near full text width.}

\begin{reviewercomment}
13. Figs. 1 and 6 are structurally very similar, which limits the additional information conveyed by Fig. 6. Moreover, why are the AI engines and network controllers absent from Fig. 6? Was the architecture revised, or were the functions of these two components absorbed by other modules in the figure? Fig. 1 and Section III also state that ``AI engines provide ML and optimisation capabilities for data-driven inference'' and that ``Finally, the network controllers translate the optimised decisions or recommendations generated by the Agentic AI layer into executable network configurations, including path provisioning, cross-domain orchestration, and policy-based closed-loop control.'' However, these functions are not clearly demonstrated in the discussions or results presented in Sections IV and V. Please provide supporting results for these claimed capabilities.
\end{reviewercomment}
\begin{authorresponse}
The two figures now serve different purposes. Fig.~1 shows the complete system architecture and the connections among practical telemetry, repositories, semantic processing, DT generation, AI services, and controllers. Fig.~6 shows the internal sequence of the new orchestrated data-generation workflow, including the functional roles, knowledge base, simulator interface, orchestrator, and retained artefacts. The AI engine and SDN controller were implemented and experimentally demonstrated in our earlier optical telemetry platform; Section~IV-A now cites that work and reports its measured transfer, inference, and reconfiguration times. The present experiments extend the platform's data path and evaluate semantic fusion and DT-based generation.
\end{authorresponse}
\change{The Fig.~1 and Fig.~6 captions and Sections~III--V now state the distinct architectural and workflow roles. Section~IV-A provides the published implementation evidence for the AI-engine and controller blocks.}

\begin{reviewercomment}
14. Are the five ``agents'' in Fig. 6 merely nodes that invoke an LLM, or are they autonomous agents? If they are agents, their internal structures should be incorporated into Fig. 6, including their tools, memory-management mechanisms, and intermediate scheduling components. Although a multi-agent architecture can improve flexibility and completeness, it also introduces inter-agent communication overhead, increases the number of LLM calls, and makes the overall framework less controllable. The authors should therefore explain why the Planner Agent, Scenario Expansion Agent, and Report Agent are implemented as agents when their assigned tasks do not appear particularly complex. If these components are not agents, the relevant descriptions should be revised and the characterization of the framework as multi-agent should be reconsidered.
\end{reviewercomment}
\begin{authorresponse}
We retain the multi-agent architecture as the experimental design and give an operational definition: Fig.~6 depicts five functional roles in an LLM-driven design, coordinated through orchestrator-owned shared state. In the instrumented feasibility run, Planner, Scenario Expansion, and Reflection invoked the LLM, while Results Analysis and Report used deterministic code for KPI aggregation and artefact packaging. Simulator wrappers, topology/equipment files, KPI definitions, and stored results are their tools and knowledge. Role separation provides explicit input/output contracts, traceable intermediate artefacts, and independently replaceable implementations. The roles exchange serialised state through the orchestrator rather than peer-to-peer messages. For this run, the observable LLM overhead was six calls, 108,308 tokens, and 26.22~s summed API latency; it does not isolate an inter-role communication ablation or establish an efficiency advantage.
\end{authorresponse}
\change{Section~V-A distinguishes the five-role architecture from the instrumented feasibility run and defines the shared state, tools, retained artefacts, and orchestration sequence. Fig.~6 depicts the role sequence and its knowledge-base, DT, and orchestrator interfaces. Table~3 reports the measured call, token, latency, cost, retry, and failure totals for that run.}

\begin{reviewercomment}
15. In Fig. 6, the font color used for the Reflection Agent is too similar to the background color, making the text difficult to read. Please avoid this color combination.
\end{reviewercomment}
\begin{authorresponse}
Corrected as requested. The replacement PDF uses high-contrast white Reflection text on a dark blue background.
\end{authorresponse}

\begin{reviewercomment}
16. Fig. 7 is not sufficiently clear and should be provided at a higher resolution.
\end{reviewercomment}
\begin{authorresponse}
Fig.~7 was regenerated at high resolution without changing the historical experimental data. Panel (a) retains the original request--scenario-expansion--DT-execution--dataset-output workflow, while panels (b)--(d) reproduce the archived GNPy results with clearer labels, axes, and legends.
\end{authorresponse}

\section*{Reviewer 3}

\begin{reviewercomment}
1. The semantic-fusion experiments are not reproducible. The manuscript does not identify the optical and wireless data used or explain how the fused dataset was constructed. Please provide sample counts, label definitions, semantic confidence scores, model architectures, training settings, and the evaluation protocol. Random record-level splitting may leak correlated samples; use temporal or session-grouped splits.
\end{reviewercomment}
\begin{authorresponse}
We replaced the random split with chronological 60/20/20 train/validation/test segments and ten-record purge gaps, yielding 1,698/560/570 evaluated samples. All thresholds, scalers, encoders, classifiers, and attackers are fitted after splitting using training data only. The manuscript now defines the 12 wireless inputs, semantic features, confidence heuristic, optical and wireless risk rules, one-step targets, model architectures, optimiser settings, ten seeds, and evaluation metrics.
\end{authorresponse}
\change{Section~IV-B and the public materialised 2,848-row/52-field benchmark now record source indices, split membership, thresholds, features, and labels.}
\evidence{The optical and wireless records were collected in different time periods because of experimental-platform scheduling. The released benchmark table records source-row indices, unified-interface outputs, split membership, and rule-derived labels.}

\begin{reviewercomment}
2. Table 1 does not establish improved privacy. V3 has the highest RAT attack accuracy (0.988), while its lower Macro-F1 cannot be interpreted without class distributions and baselines. Please add majority and random baselines, attacker performance on raw telemetry, multiple attacker models, repeated runs, and confidence intervals. Until then, use ``privacy-aware'' rather than ``privacy-preserving.''
\end{reviewercomment}
\begin{authorresponse}
We added all requested comparisons. CPE Macro-F1 is the primary privacy metric because the RAT test set is highly imbalanced. Four attacker families are evaluated over ten seeds. Raw, PCA, random projection, majority, and stratified-random baselines are included, with 95\% confidence intervals. V3 achieves strongest-observed CPE attacker Macro-F1 $0.2382\pm0.0113$, compared with 0.2757 for raw features; PCA3 reaches $0.2174\pm0.0059$ but with lower utility.
\end{authorresponse}
\change{The manuscript consistently uses ``privacy-aware'' and states that there is no formal or universal privacy guarantee.}

\begin{reviewercomment}
3. The LLM framework is demonstrated but not evaluated. The GNPy parameter space could be enumerated by a deterministic script, yet no baseline is provided. Report the LLM model, prompts, settings, trials, success rate, reflection iterations, human interventions, failures, runtime, and token cost. Alternatively, limit the claims to a feasibility demonstration.
\end{reviewercomment}
\begin{authorresponse}
We selected the suggested feasibility scope. The full instrumented-run details and deterministic baseline are reported in our response to Reviewer~1's LLM-workflow comment. The article retains the model/settings, six-call summary, cost/latency, baseline, and manual-recovery boundary. Complete prompts, responses, failure logs, and intervention chronology are released but moved out of the main narrative. On the wireless side, the 48-configuration ns-3 dataset is evaluated for structural and numerical correctness; this supports executable data generation but is not presented as evidence that an LLM is superior to a deterministic workflow.
\end{authorresponse}
\evidence{The final Reflection response requested another refinement at the configured two-iteration limit and was not recomputed after the corrected replay; convergence and autonomous repair are therefore not claimed. No repeated LLM trial or role ablation is claimed.}

\begin{reviewercomment}
4. The generated data lack independent correctness validation. Runtime checks may catch execution failures but not plausible results from incorrect configurations. Add an audit using independently reproduced samples, a hand-written generator, established GNPy examples, or field measurements. Identify the repository commit and report simulator versions and material configuration assumptions.
\end{reviewercomment}
\begin{authorresponse}
For GNPy, 96 cases stratified by path, joint modulation/slot-width setting, power, and channel load were regenerated independently. A pinned environment using official GNPy v2.12 (commit \texttt{7ce66501\allowbreak{}0970f57e\allowbreak{}46672db1\allowbreak{}ec0864ae\allowbreak{}448077e6}) completed 96/96. Its mean and maximum absolute GSNR differences from the archive were 0.00015 and 0.00050~dB. A schema-corrected GNPy v2.14.2 run completed 96/96, with corresponding differences of 0.628 and 1.435~dB, demonstrating version sensitivity. Both arms retain EDFA/ROADM warnings; ``successful'' therefore means numerically reproducible under declared assumptions, not physically calibrated.

For wireless data, we independently parsed the FlowMonitor XML from all 48 archived ns-3.44 runs, covering 624 raw flow rows and 432 application-flow rows. All application packet counters and FlowMonitor lost-packet fields matched the exported KPI files exactly. Continuous KPI differences were limited to XML time-serialisation precision: at most $5.57\times10^{-5}$~Mbps for throughput, $3.16\times10^{-3}$~ms for mean delay, and $1.10\times10^{-5}$~ms for mean jitter. Three representative configurations rebuilt and replayed with nine application flows each and zero KPI delta; a same-seed configuration repeated identically; and an independently written ns-3 reference passed five topology/configuration checks and all 63 keyed KPI comparisons. These tests support implementation consistency and reproducibility for the declared LTE/EPC, Wi-Fi 802.11n, and CSMA-based LiFi-surrogate scenario. They do not establish physical LiFi fidelity, calibration to the practical testbed, or a universal technology ranking. FlowMonitor lost packets are reported as a simulator statistic rather than terminal packet loss at the finite stop time.
\end{authorresponse}
\change{Section~V-C and the dataset-summary table report the generated-data scope and validation boundaries; the numerical reconstruction, replay, and configuration details are documented here and under \texttt{MajorRevision/ns3\_wireless\_validation/} in the public revision repository.}

\begin{reviewercomment}
5. Separate implemented, evaluated, demonstrated, and proposed components in the abstract.
\end{reviewercomment}
\begin{authorresponse}
The revised Abstract distinguishes the implemented cross-domain architecture and unified interface, the separately timed practical acquisitions, the evaluated semantic-fusion benchmark, the LLM/DT workflow, the deterministic baseline, and the versioned numerical replay. The inherited AI-engine and controller implementation is described and cited in Sections~III and IV-A rather than presented as a new experimental result.
\end{authorresponse}

\begin{reviewercomment}
6. Sections 3 and 4.A overlap; shorten Section 3 and use the space for methodology.
\end{reviewercomment}
\begin{authorresponse}
Section~III now gives the two data workflows, the orchestrator relationship, and the boundary between inherited platform functions and the contributions of this study. Section~IV-A contains the component-level implementation details and the evidence for the inherited AI-engine/controller services. The repeated database, telemetry, and capability descriptions were removed.
\end{authorresponse}

\begin{reviewercomment}
7. Explain the relevance of the AC/DC power-supply detail or remove it.
\end{reviewercomment}
\begin{authorresponse}
Removed as requested because it is not needed for the dataset or DT claims.
\end{authorresponse}

\begin{reviewercomment}
8. Support or soften the claim that $-5$~dBm provides optimal performance.
\end{reviewercomment}
\begin{authorresponse}
We now call $-5$~dBm the operating point used for the practical campaign. The synthetic GNPy results are described as route-dependent sensitivity over three tested powers; no global optimum is claimed.
\end{authorresponse}

\begin{reviewercomment}
9. Standardise British or American English throughout the manuscript. The text currently mixes forms such as ``centralised/centralized,'' ``optimisation/optimization,'' ``behaviour/behavior,'' and ``fibre/fiber.'' Select one spelling convention and apply it consistently...
\end{reviewercomment}
\begin{authorresponse}
We selected British English and standardised active prose (for example, organised, optimisation, behaviour, fibre, centralised). Bibliographic titles and software identifiers retain their source spelling.
\end{authorresponse}

\begin{reviewercomment}
10. Use ``ns-3'' consistently and standardise heading capitalisation.
\end{reviewercomment}
\begin{authorresponse}
Corrected throughout. The simulator is written consistently as ``ns-3'', and the synthetic branches are identified as LTE/EPC, Wi-Fi 802.11n, and a CSMA-based LiFi surrogate. The separate practical wireless dataset retains its measured 5G NR, Wi-Fi~6, and LiFi terminology, and is clearly distinguished from the packet-level synthetic experiment.
\end{authorresponse}

\begin{reviewercomment}
11. Replace or remove the placeholder DOI on page 1.
\end{reviewercomment}
\begin{authorresponse}
Removed. The journal class inserts \texttt{http://dx.doi.org/10.1364/ao.XX.XXXXXX} by default, even when the template DOI line in the manuscript is commented. It also inserts \verb|Compiled \today| through its default date field. We now explicitly set both \verb|\doi{}| and \verb|\dates{}|, rebuild the article, and verify the first-page PDF text. The publisher can insert the assigned DOI and publication dates after acceptance.
\end{authorresponse}

\begin{reviewercomment}
12. Correct ``includign BER,'' ``based over the NDFF topology,'' and ``formats,i.e.''.
\end{reviewercomment}
\begin{authorresponse}
Corrected as requested. We also performed a final mechanical check of active prose, citations, references, terminology, and heading capitalisation.
\end{authorresponse}

\section*{Availability of revision evidence}

The revised manuscript source, compiled PDF, figures, response letter, Croissant metadata, semantic evaluation outputs, deterministic baseline, GNPy replay, LLM trace, and synthetic wireless validation artefacts are collected in the cited revision repositories. The datasets and associated metadata/documentation are licensed under CC BY-NC 4.0, and the article uses ``openly available'' consistently with those terms. Software and cited external datasets retain their stated licences; simulator-calibration and workflow-trace boundaries are documented with the corresponding evidence.

\end{document}
