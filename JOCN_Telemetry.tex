\documentclass[9pt,twocolumn,twoside]{osajnl}
\usepackage{comment}
\usepackage{threeparttable}
\usepackage{siunitx}
\usepackage{subcaption}
\usepackage{threeparttable}
\usepackage{siunitx}
\usepackage{multirow}
\usepackage{adjustbox}
\usepackage{ragged2e}
\setcounter{secnumdepth}{3}
\usepackage{stfloats}
\usepackage{afterpage}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{amsmath}
\usepackage{array}
\usepackage{lipsum}
% \usepackage{subfigure}
\usepackage[normalem]{ulem}
\journal{jocn} 
\newcommand{\red}[1]{\textcolor{red}{#1}}
\usepackage{afterpage}
\usepackage{makecell} % 在导言区添加这一行以引入宏包
\usepackage{tabularx} % 导言区加载 tabularx 宏包
\usepackage{cleveref}
\usepackage{xurl}
\Urlmuskip=0mu plus 1mu

% Set the article type for journal submissions. Comment out this line for Optica Open preprint submissions.
\setboolean{shortarticle}{false}
% true = letter / tutorial
% false = research / review article

\title{{Open-Source Cross-Domain Datasets Enabled by LLM-Driven Digital Twins and Network Telemetry}}

\author[1]{Sen Shen\textsuperscript{\textdagger,}}
\author[1]{Wanxin Zhao\textsuperscript{\textdagger,}}
\author[1]{Xuewen Chen\textsuperscript{\textdagger,}}

\author[1]{Jing Han}
\author[1]{Xueqing Zhou}
\author[1]{Ruizhi Yang}
\author[1]{Peiyun Ge}
\author[1]{Zhuojun Cai}
\author[1]{Vaigai Yokar}
\author[1]{Yulei Wu}
\author[1]{Shuangyi Yan*}
\author[1]{Dimitra Simeonidou}


\affil[1]{Smart Internet Lab, Faculty of Science and Engineering, University of Bristol, Bristol, United Kingdom.\newline
\textsuperscript{$\dagger$}These authors contributed equally to this work.}

\affil[*]{shuangyi.yan@bristol.ac.uk}




%% The publisher will insert the publication dates. The class supplies a
%% compilation date by default, so it must be explicitly cleared for review.
\dates{}

%% To be edited by editor
% The publisher will insert the final DOI after acceptance.  The class supplies
% a template DOI by default, so it must be explicitly cleared for submission.
\doi{}

\begin{abstract}

% Machine learning (ML)-enabled network intelligence requires diverse and accessible data across heterogeneous network domains, whereas open-source datasets jointly covering optical transport, fibre sensing, and wireless access remain limited. This paper presents an open-source cross-domain dataset framework that combines practical measurements with large-language-model (LLM)-driven multi-agent digital twins (DTs) for automated data generation in optical–wireless networks. We provide practical datasets collected from experimental testbeds, including long-term optical transmission performance data over the UK National Dark Fibre Facility, fibre communication and sensing data over deployed urban links, and multi-access wireless performance data involving 5G, Wi-Fi, and LiFi. To support cross-domain fusion, three semantic-module designs are investigated, with the privacy-aware bottlenecked module achieving the most favourable overall privacy–utility trade-off. Furthermore, the proposed LLM-driven multi-agent DT framework translates data-generation requests into executable simulation workflows and iteratively refined dataset outputs. In a GNPy-based optical QoT use case, it produces 8,160 valid samples from 8,192 executed scenarios. The proposed datasets and generation framework provide an open foundation for ML development in future cross-domain network intelligence.


Machine-learning-enabled network research requires both practical telemetry and controllable synthetic data, but optical transport, fibre sensing, and wireless access data are usually collected and released separately. We present an openly available cross-domain dataset framework with two complementary parts: independently acquired practical datasets organised through common metadata, and generated datasets produced through an LLM-driven multi-agent digital-twin (DT) workflow. ``Cross-domain'' denotes common representation, repository, and task-level fusion; it does not imply contemporaneous end-to-end measurement. On a constructed optical--wireless interoperability benchmark with chronological splitting, a three-dimensional privacy-aware wireless representation achieves binary service-risk Macro-F1 $0.646\pm0.005$ while moderately reducing observed CPE-attribute leakage relative to raw telemetry. In the optical generation case, 8,192 GNPy execution records correspond to 6,144 unique configurations, exactly matching an independent deterministic enumerator. A 96-case replay establishes historical numerical compatibility and quantifies sensitivity to a current GNPy version. The LLM workflow is therefore presented as a feasibility demonstration, while the semantic module is evaluated as an empirical privacy--utility trade-off rather than a privacy guarantee.


\end{abstract}

\setboolean{displaycopyright}{false} % Do not include copyright or licensing information in submission.

\begin{document}

\maketitle

\section{Introduction}

In recent years, machine learning (ML) has been widely investigated in telecommunication networks to support intelligent and autonomous network operation. In optical networks, ML has been applied to quality of transmission (QoT) estimation, traffic prediction, anomaly detection, and resource allocation~\cite{villa2023IEEEAccess}. In wireless networks, ML-based methods have also been explored for radio resource management, multi-access optimisation, traffic prediction, mobility management, and service quality assurance~\cite{sun2019IEEE}. These advances indicate a clear trend towards AI-native network intelligence, where data-driven models support monitoring, optimisation, and closed-loop control.

% Despite these advances, the performance and generalisation capability of ML models strongly depend on the availability of representative and high-quality datasets. In practical telecommunication networks, however, obtaining sufficient data remains challenging. Real operational data are often difficult to access because they contain sensitive information related to network topology, device configurations, service conditions, customer traffic, and operator policies. As a result, network operators are generally reluctant to disclose such data. Although several open-source datasets have been released for optical or wireless network research, most existing datasets are still limited to a single network domain, a specific task, or a restricted experimental scenario. This limits their applicability to future intelligent networks, where decisions increasingly depend on joint observations across multiple domains.

However, the effectiveness of ML models strongly depends on representative data. Operational network records can expose topology, device configurations, traffic, and service conditions, which limits public release. Existing optical and wireless datasets~\cite{zhai2024alibaba,filer2016microsoft} also tend to address a single domain, task, or campaign. This restricts studies of services whose behaviour depends on both wireless access and optical transport. Heterogeneous infrastructures, telemetry interfaces, data formats, time scales, and privacy requirements make such datasets difficult to organise and combine. Synthetic data can complement practical measurements by covering controllable or rare conditions, although DT-based generation still requires substantial effort to specify scenarios, execute simulations, and validate the results.

% The lack of open-source cross-domain datasets is particularly critical for converged optical and wireless networks. In such networks, wireless access domains and optical transport domains are tightly coupled to support end-to-end services. Service performance degradation may originate from wireless access conditions, optical transmission impairments, traffic dynamics, or their combined effects. Therefore, cross-domain data are required to support tasks such as end-to-end service monitoring, cross-domain fault diagnosis, resource coordination, and AI-assisted orchestration. However, constructing such datasets is difficult because optical and wireless domains rely on heterogeneous infrastructures, telemetry interfaces, data formats, time scales, and control mechanisms. In addition, raw wireless telemetry may contain privacy-sensitive information, while optical-domain data often involve confidential topology and configuration details. These factors make cross-domain data collection, alignment, fusion, and sharing substantially more challenging than single-domain dataset construction.

% To address these challenges, this paper presents an open-source cross-domain dataset framework enabled by network telemetry, semantic data abstraction, and LLM-driven multi-agent digital twins. The proposed framework integrates distributed and centralised data repositories, Kafka-based telemetry pipelines, digital twins, AI engines, and network controllers to support scalable data collection, cross-domain data fusion, and automated dataset generation. Practical datasets are collected from real optical and wireless testbeds, covering optical transmission performance monitoring, fibre sensing, and multi-access wireless network performance. These datasets provide practical data sources for studying network monitoring, sensing, prediction, and cross-domain optimisation.

To address these challenges, this paper presents an openly available cross-domain dataset framework that supports both practical and generated datasets: practical data are organised through network telemetry and semantic abstraction, whereas synthetic data are demonstrated through an LLM-driven multi-agent DT workflow. The term cross-domain has two precise meanings in this work. First, independently acquired optical-transport, fibre-sensing, and wireless-access datasets are exposed through a common repository and metadata structure. Second, a constructed interoperability benchmark combines optical and wireless feature sequences to test whether compact wireless representations remain useful to a common downstream model. Because the practical campaigns were conducted at different times, this benchmark does not represent simultaneous end-to-end measurements and is not used to infer a physical causal relationship between the domains.

The main contributions are as follows. (1) We consolidate practical optical-transport, fibre-sensing, and multi-access wireless datasets with their acquisition context, formats, sampling information, and repository access terms. The telemetry platform builds on our previous implementations~\cite{shen2025telemetry,Shen2026OFCdata}; the present contribution is their dataset-level organisation and cross-domain interface. (2) We specify and evaluate hand-crafted and learned wireless semantic representations using chronological partitions, train-only preprocessing, repeated runs, conventional baselines, and multiple privacy attackers. (3) We demonstrate an LLM-driven multi-agent interface to GNPy and ns-3 DT engines and compare its optical configuration set with an independent deterministic enumerator. (4) We assess simulator provenance and replayability with historical-compatibility and current-version GNPy checks, separating workflow feasibility, numerical reproducibility, and physical calibration.

% In addition to practical data collection, this work introduces semantic data abstraction for efficient and privacy-aware cross-domain fusion. Instead of directly transmitting raw wireless telemetry to the cross-domain repository, semantic modules transform wireless-domain observations into compact task-relevant representations. Three semantic module designs are investigated, including a hand-crafted semantic module, a learned latent semantic module, and a privacy-aware bottlenecked semantic module. Their performance is evaluated from both utility and privacy perspectives under a unified cross-domain fusion framework. The results show that semantic abstraction can support cross-domain learning while reducing unnecessary exposure of wireless-side information.


% Furthermore, this paper proposes an LLM-driven multi-agent DT framework for automated cross-domain data generation. The framework uses collaborative agents to interpret high-level user requests, decompose data-generation tasks, expand simulation scenarios, invoke DT execution engines, analyse generated results, and refine the search space. By combining LLM-driven reasoning with DT-based simulation, the framework reduces manual effort in scenario design and enables scalable generation of synthetic datasets under configurable network conditions. The feasibility of the framework is demonstrated through DT-based data-generation use cases in optical and wireless network scenarios.

% The main contributions of this paper are summarised as follows. First, we propose a unified open-source cross-domain dataset framework for converged optical and wireless networks, integrating telemetry, distributed and centralised data repositories, DTs, AI engines, and network controllers. Second, we release practical datasets collected from real optical and wireless testbeds, covering transmission performance monitoring, fibre sensing, and multi-access wireless performance. Third, we design and evaluate semantic modules for privacy-aware wireless data abstraction and cross-domain fusion. Fourth, we propose an LLM-driven multi-agent DT framework that automates scenario construction, simulation execution, result analysis, and iterative dataset refinement.

The remaining sections are organised as follows. Section~II reviews cross-domain data, semantic abstraction, DTs, and LLM-driven network automation. Section~III presents the unified framework, Section~IV describes the telemetry architecture and practical datasets, Section~V presents the LLM-driven multi-agent DT demonstration, and Section~VI concludes the paper.

% \section{Related Work}
% \subsection{ML Applications}
% With the successful application of ML in natural language processing, ML techniques have been widely applied to optical and wireless networks over the past decade for performance prediction, resource allocation, and network optimisation. These advances have accelerated the evolution towards AI-native networks. More recently, LLMs with strong reasoning and task-planning capabilities, have further enhanced network automation and reduced human intervention.

% \subsubsection{Traditional ML in Optical and Wireless Networks}

% Traditional ML can be categorised into four distinct groups: supervised learning, unsupervised learning, and reinforcement learning. Supervised learning including Random Forest, Decision Tree, Support Vector Machine and neural networks, is widely used in QoT estimation and traffic prediction. For example, in optical networks, an RF-based ML equaliser was developed for DP-64QAM coherent optical transmission, achieving deep-learning-level equalisation performance with substantially lower computational complexity. Also, an SVM-based ML approach was introduced to estimate lightpath QoT in optical transport networks, achieving high classification accuracy and significantly reduced computation time compared with semi-analytical methods. And an ANN-based multi-channel QoT prediction framework was demonstrated using real field-trial monitoring and configuration data, showing that the ML-based method can achieve accurate Q-factor estimation for dynamic optical network optimisation. Similarly, an SVM-based secure wireless communication framework was introduced by constructing signal-derived features and artificial training data from wireless propagation signals to detect active eavesdropping at the physical layer. In wireless networks, artificial neural networks have been extensively used to learn complex relationships from network data and support intelligent functions such as prediction, classification, resource optimisation, and autonomous network management.

% Unsupervised learning such as K-means, autoencoder, DBSCAN, is widely used in tasks including anomaly detection and clustering. For example, A DBSCAN-based unsupervised anomaly detection loop was deployed in a microservice-based optical SDN controller, achieving scalable telemetry-driven monitoring without labeled training data and supporting up to 960 optical channels with 5–20 ms inference latency. Also, unsupervised DNN-based power control has been applied to energy-efficient wireless networks by directly optimising fractional energy-efficiency objectives through primal-dual learning, achieving real-time inference without requiring labeled solutions from conventional optimization algorithms. Compared with supervised and unsupervised learning, RL is mostly used in decision and control tasks. In optical networks, DRL is widely used in RMSA problem. For example, it has been applied to dynamic RMSCA in multi-core-fiber SDM-EONs, where PPO-based agents learn adaptive routing and core-allocation policies to improve spectrum utilization and reduce blocking probability under crosstalk constraints. In wireless networks, DRL is widely used to learn adaptive resource allocation and control policies under dynamic channel, traffic, and mobility conditions. In wireless networks, DRL is widely used to learn adaptive resource allocation and control policies under dynamic channel, traffic, and mobility conditions. For multi-server MEC networks, DRL has been used to support long-term resource planning, where DQN and multi-agent DDPG enable resource reservation and server cooperation, while optimization-based policies handle task offloading, transmission power allocation, and computing resource distribution.


% \subsubsection{LLMs in Networks}

% \subsection{Network Telemetry}


\section{Related Work}

\subsection{Cross-Domain Data Availability and Network Telemetry}
 
With the increasing use of ML in telecommunication networks, data have become a key enabling resource for model training, validation, inference, and updating. In cross-domain optical--wireless networks, practical telemetry is particularly valuable because it captures communication performance, device states, service requests, and environmental variations under real operating conditions. In optical transport and fibre-sensing systems, such data may include bit error rate (BER), optical signal-to-noise ratio (OSNR), Q-factor, optical power, route information, configuration records, device alarms, state-of-polarisation (SOP) evolution, backscatter traces, distributed acoustic sensing signals, and vibration-related features~\cite{he2023isac,zhao2025vibration,zhao2026train}. In wireless access networks, standardised telemetry may include throughput, packet loss, delay, signal strength, cell load, mobility, traffic distribution, and per-device access statistics~\cite{3gpp28552,etsi128554}. The value of these heterogeneous data lies not only in individual measurements, but also in their temporal alignment and shared service context across optical transport, fibre sensing, and wireless access domains.


However, practical cross-domain data are difficult to collect, align, and share. Optical transport, fibre sensing, and wireless access systems rely on different infrastructures, telemetry interfaces, data formats, and time scales. In addition, operational data may expose real network topology, configuration, device states, customer traffic, service conditions, and operator policies, thereby limiting public data sharing. Consequently, only a limited number of open optical-network datasets are available. Representative examples include the Microsoft wide-area optical backbone dataset for operational network-performance analysis~\cite{filer2016microsoft,delezoide2020field}, the Fraunhofer HHI QoT dataset collection for ML-assisted QoT estimation in elastic optical networks~\cite{bergk2022qotdataset}, and the Alibaba disaggregated production-network dataset for field-data-based QoT estimation and parameter optimisation~\cite{zhai2024alibaba,he2023qot}. Recent dataset-oriented efforts have further covered space-division-multiplexing networks, fibre sensing, optical-device modelling, and radio-over-fibre systems~\cite{akbari2025sdmdatasets,li2026dasdataset,yuan2026soadataset,zhao2026mmwavearof}. Nevertheless, most existing datasets remain limited to a single domain, topology, task, or measurement campaign, and rarely provide time-aligned optical-transport, fibre-sensing, and wireless-access observations under a common service context.

Network telemetry provides the basis for scalable data collection~\cite{rfc9232}. Model-driven and streaming telemetry solutions based on NETCONF/YANG, gRPC, gNMI, and publish--subscribe mechanisms have been developed to expose network states and performance measurements~\cite{rfc9232,gnmi2018}. In optical networks, prior studies have demonstrated telemetry services for real-time monitoring in disaggregated optical networks, streaming-telemetry-assisted diagnosis, Kafka-based optical monitoring frameworks, and unified monitoring platforms integrating telemetry collection, time-series storage, configuration databases, and streaming pipelines~\cite{paolucci2018telemetry,tanaka2021diagnosis,sgambelluri2022kafka,shen2025telemetry}. In wireless networks, O-RAN and AI-native RAN architectures also rely on multi-source telemetry to expose radio, traffic, and service context to intelligent controllers and AI/ML applications~\cite{mahmoud2026oranintelligence}. However, most existing telemetry frameworks are designed for specific domains, vendor interfaces, or monitoring tasks. Cross-domain data collection, time alignment, format consistency, repository management, and service-level interpretation across optical transport, fibre sensing, and wireless access domains remain insufficiently addressed.

\subsection{Semantic Data Abstraction for Privacy-Aware Cross-Domain Fusion}

Cross-domain dataset generation requires not only collecting heterogeneous telemetry, but also deciding which information should be shared, stored, and fused. Directly aggregating raw wireless telemetry into a central repository may introduce unnecessary communication and storage overhead while exposing device-specific or service-sensitive information. This issue is particularly critical in multi-access wireless networks, where raw telemetry may reveal device identity, access behaviour, traffic patterns, mobility states, and service conditions~\cite{8792139}. Therefore, practical optical--wireless data fusion requires compact and task-relevant representations that preserve useful information for downstream analysis while reducing the exposure of sensitive raw observations.

Task-oriented semantic communication provides a useful principle for such abstraction by transmitting information relevant to downstream tasks rather than reconstructing all source observations with equal fidelity~\cite{xie2022task,ma2023explainable}. Within telemetry systems, semantic representations can be constructed from expert-designed indicators or learned encoders. In particular, information-bottleneck methods can learn compact latent variables that preserve task-relevant information while discarding irrelevant input details~\cite{alemi2017vib}. Such representations can summarise wireless load, link stability, access diversity, congestion risk, and service degradation, thereby reducing data volume and enabling more efficient fusion with optical transmission and fibre-sensing measurements. However, compact representations are not automatically privacy preserving, because sensitive attributes may still be inferred from the transmitted features. Privacy-aware representation learning therefore requires information bottlenecks, noise injection, adversarial objectives, or representation regularisation to balance downstream utility against sensitive information leakage~\cite{alemi2017vib,edwards2016censor,madras2018fair,wang2024privacysemantic}.

Existing studies on semantic-communication and privacy-aware representation mainly focus on single-domain communication data, end-to-end transmission efficiency, or downstream task accuracy. Limited work has investigated how wireless telemetry should be abstracted for cross-domain optical--wireless dataset generation, or how such representations should be evaluated jointly in terms of cross-domain utility, data reduction, and privacy exposure. This motivates the design of wireless semantic modules that transform raw multi-access telemetry into compact task-relevant representations for privacy-aware fusion with optical transmission and fibre-sensing data.

\subsection{Digital Twins and LLM-Driven Data Generation}

The preceding subsections concern the collection and selective sharing of practical telemetry. Synthetic data address a different limitation: even a well-instrumented testbed cannot economically cover every topology, loading condition, device setting, or rare failure. Synthetic data can therefore complement, rather than replace, independently collected measurements by expanding scenario coverage and providing labels under controllable conditions. Network DTs provide a natural execution tool because they emulate network behaviour under configurable topology, traffic, device, channel, and service conditions~\cite{zhuge2023dt}.

In optical networks, GNPy provides an open and vendor-independent physical-layer modelling framework for emulating optical paths under configurable topology, equipment, channel loading, and transmission-parameter settings~\cite{curri2022gnpy}. Recent studies have used GNPy-based DTs to generate optical-network datasets under normal and degraded conditions, such as amplifier degradation, span-loss variation, and component misconfiguration~\cite{mohamed2025dtfailuredb,mohamed2025mlagentsdt}. In wireless networks, simulators and DT platforms, such as ns-3 and Colosseum, support repeatable packet-level, radio frequency, and protocol-stack emulation~\cite{riley2010ns3,polese2024colosseum}, while generative-AI-enabled wireless DTs have been explored for 6G modelling and scenario augmentation~\cite{tao2024wirelessdt,chai2024genaidt}. These studies demonstrate the value of DTs for controllable data generation. Nevertheless, DT-based dataset generation still requires substantial manual effort to define objectives, configure scenarios, execute simulations, validate results, and refine parameter settings. This manual process becomes costly and difficult to scale in cross-domain optical--wireless environments, where different DT tools rely on different abstractions, interfaces, and configuration formats.

Recent studies have begun to integrate LLMs, AI agents, and DTs to reduce the manual burden of network automation. In optical-network DT research, modelling, telemetry, self-learning, and lifecycle management have been identified as key components for intelligent optical networks~\cite{wang2024dtreview}. GPT-enabled DT assistants have also been demonstrated to interpret user intentions and interact with DT functions for autonomous optical-network management~\cite{zhang2024gptdtassistant}. In addition, DTs have been used to provide updated network states and pre-verify LLM-generated strategies before deployment, with demonstrations covering performance optimisation, protection switching, and recovery after fibre cuts~\cite{song2025llmdt}. Other studies have explored fine-tuned local agents, DT-aided wavelength-loading control, AI-agent integration, and field-trial demonstrations for optical-network lifecycle automation~\cite{sun2025localaiagents,liu2024autodtwave,zhuge2025dtaiagent,liu2026fieldtrial}. These studies demonstrate the potential of LLMs and AI agents to interact with DTs, but their focus remains on network operation and management rather than autonomous dataset generation.

The cited DT assistants and AI-agent studies automate parts of modelling and network operation, but they still rely on explicitly supplied objectives and configuration ranges~\cite{zhang2024gptdtassistant,song2025llmdt,sun2025localaiagents,liu2024autodtwave}. This leaves dataset generation as a distinct workflow-design problem. The present work therefore studies an LLM-driven interface that translates a high-level request into structured tasks, invokes optical and wireless DT engines, analyses generated KPIs, and proposes refinements. Section~\ref{sec:LLM} evaluates this as a feasibility demonstration and includes a deterministic configuration-enumeration baseline; it does not claim that an LLM is necessary for exhaustive enumeration.








\begin{figure*}[t]
    \centering
    \includegraphics[width=0.95\linewidth]{Figures/architecture_v1.png}
    \captionsetup{justification=centering}
    \vspace{-0.1cm}
    \caption{Scope map of the complete dataset framework. The left branch organises independently acquired practical datasets; the right branch places the demonstrated DT data-generation workflow within a broader prospective network-intelligence context.}
    \label{fig:proposed architecture.png}
    \vspace{-0.3cm}
\end{figure*}


\section{Unified Framework for Cross-Domain Dataset Collection and LLM-Driven Data Generation}

Figure~\ref{fig:proposed architecture.png} illustrates a unified framework for cross-domain dataset collection and LLM-driven data generation in wireless–optical network environments based on our previous work~\cite{shen2025telemetry}~\cite{Shen2026OFCdata}. The bottom layer represents the physical infrastructure of a general end-to-end network scenario, in which wireless access domains are interconnected through an optical transport network. This abstraction captures a practical multi-domain networking environment where heterogeneous devices, protocols, and control entities coexist across wireless and optical segments.

The framework has two complementary branches. The practical-data branch collects and publishes independently acquired telemetry, while the generated-data branch converts a user request into simulator inputs and retained artefacts. Component-level storage and telemetry details are deferred to Section~\ref{sec:cross-domain-telemetry}.

In this paper, the implemented path comprises telemetry export, semantic evaluation, orchestrated task construction, and GNPy/ns-3 execution. AI engines and network controllers remain contextual integration points; QoT prediction, path optimisation, and physical-network enforcement by those blocks are not evaluated.

Figure~\ref{fig:proposed architecture.png} is therefore a scope map, whereas Fig.~\ref{fig:architecture_LLM} later gives the evaluated data-generation sequence. The orchestrator dispatches that sequence but is not claimed to control every component in Fig.~\ref{fig:proposed architecture.png}.

The detailed design of the cross-domain telemetry architecture across optical and wireless networks is presented in Section~\ref{sec:cross-domain-telemetry}, while the LLM-driven multi-agent DT framework for cross-domain data generation is further elaborated in Section~\ref{sec:LLM}.

\section{Cross-Domain Telemetry Architecture Across Optical and Wireless Networks}
\label{sec:cross-domain-telemetry}

\subsection{Proposed Cross-Domain Telemetry Architecture}
\label{subsec:4.A}

Figure~\ref{fig:cross-domain telemetry.png} presents the cross-domain telemetry architecture developed from our earlier telemetry platform~\cite{shen2025telemetry}. In this study, the implemented path comprises device-data acquisition, time-series and configuration storage, Kafka transfer, exported-record semantic evaluation, and the DT interfaces demonstrated in Section~\ref{sec:LLM}. The AI-engine, controller, local-inference, and closed-loop control blocks are prospective integration points. No scalability benchmark, controller deployment, or cryptographic privacy guarantee is claimed. This scope applies throughout this subsection.

\subsubsection{Optical Transport Network Physical Layer}

At the bottom of Fig. \ref{fig:cross-domain telemetry.png}, the physical layer of the optical transport network consists of multiple interconnected optical nodes, including aggregation and intermediate nodes. These nodes represent practical optical transport infrastructure, in which NEs such as transponders, fibre links, optical switches, reconfigurable optical add-drop multiplexers (ROADMs), and optical amplifiers generate heterogeneous operational data.

The collected optical domain data can be categorised into three types: performance monitoring metrics, equipment parameters and topology information, and fibre sensing data. Performance monitoring metrics include receiver-side BER, OSNR, received optical power, amplifier input/output power levels, and per-port optical power measurements from optical switches. Equipment parameters and topology information include transmitter-side launch power, wavelength allocation, modulation format, amplifier gain, node configuration, and link information. Fibre sensing data include SOP, chromatic dispersion (CD), polarisation mode dispersion (PMD), and backscatter traces. These data provide communication- and physical-layer visibility, supporting applications such as QoT estimation, anomaly detection, environment-aware operation, and proactive maintenance.

% These data provide visibility at both the communication and physical layers. Performance monitoring metrics support ML applications such as QoT estimation, whereas fibre sensing data enable environment-aware network operation, anomaly detection, and proactive maintenance.


\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{Figures/cross-domain telemetry.png}
    \captionsetup{justification=centering}
    \vspace{-0.1cm}
    \caption{Telemetry and repository view of the framework. Solid data paths correspond to implemented collection/storage functions; DT, AI-engine, and controller blocks indicate integration points, not evaluated closed-loop functions in this paper.}
    \label{fig:cross-domain telemetry.png}
    \vspace{-0.3cm}
\end{figure*}


\subsubsection{Distributed Node-Level Databases}
Local databases store node- or domain-specific telemetry. Continuously sampled BER, OSNR, optical-power, and sensing streams are stored in InfluxDB, whereas topology and equipment configurations are stored in PostgreSQL.

\subsubsection{Telemetry Service Design}

The implemented telemetry service ingests available NETCONF, gRPC/gNMI, REST/HTTP, and vendor-interface records through Telegraf or source-specific collectors and writes timestamped values to the local databases.


For repository transfer, selected local records are published to Kafka topics and consumed by the central repository. Campaign and source identifiers are retained during aggregation so that independently acquired records are not mistaken for naturally aligned observations.

% This design decouples local data collection from centralised data consumption. Distributed databases act as domain-level or node-level data producers, while the centralised repository serves as the integration point for cross-domain data fusion. Upper-layer applications do not directly retrieve data from every distributed database; instead, they access the aggregated and aligned data from the centralised repository. This improves system scalability, simplifies application development, and provides a consistent data interface for network-wide analytics and control.

Before wireless data are shared with the central repository, the evaluated semantic modules transform raw telemetry into compact task-related representations. Their design and evaluation are detailed in Section~\ref{subsec:SemanticModules}.

\subsubsection{Centralised Cross-Domain Data Repository}

The central repository provides a common access point for selected monitoring streams, topology, equipment, and service metadata. Importantly, a common repository does not imply that all released campaigns are time aligned: timestamps and campaign identifiers are retained so users can distinguish contemporaneous records from independently acquired datasets.

% This selective aggregation mechanism improves scalability, avoids unnecessary centralization of large-volume telemetry, and provides the data foundation for network-wide visibility and cross-domain decision-making.

\subsubsection{Network Intelligence and Controllers}

Above the repository, DTs provide the execution environments used in Section~\ref{sec:LLM}; AI engines and network controllers show potential consumers of practical and generated data.

% The AI engine platform provides network-level AI/ML capabilities, including data ingestion and preprocessing, task specification, model composition, training or retraining, deployment and inference, monitoring, and virtualisation. Containerised execution environments, such as Kubernetes, and model-management tools, such as MLflow, can support scalable AI/ML lifecycle management. By leveraging both real telemetry data from the centralised repository and synthetic data generated by digital twins, AI engines can perform tasks such as QoT estimation, anomaly detection and traffic prediction.

% Network controllers translate the outputs of AI engines and DT-based validation into executable network actions, including path provisioning, resource allocation, device reconfiguration, and closed-loop control across optical and wireless domains. By combining data repositories, DTs, AI engines, and controllers, the proposed architecture enables an end-to-end intelligent control loop, in which telemetry data are collected from the physical infrastructure, fused across domains, analysed by AI/ML models, validated through DTs, and finally applied to the network through controllers.

\subsubsection{Wireless Access Network Domains}

The wireless-domain blocks in Fig.~\ref{fig:cross-domain telemetry.png} show where 5G, Wi-Fi, and LiFi testbed telemetry enters the common repository and semantic interface.

% Through the semantic module and Kafka-based telemetry pipeline, selected wireless-domain information is forwarded to the centralised cross-domain repository, where it is fused with optical-domain telemetry for global optimisation and unified network control.


\subsection{Semantic Modules for Cross-Domain Data Fusion}
\label{subsec:SemanticModules}

The semantic module is evaluated as an interface for converting wireless telemetry into compact representations. We compare V1, a hand-crafted representation; V2, a learned eight-dimensional latent representation; and V3, a learned three-dimensional bottleneck with optional training-time noise. The term privacy-aware refers to an evaluated design objective---dimensionality and noise are varied and attribute leakage is measured---rather than a formal privacy guarantee.

%Fig.\ref{fig:telemetry service design.png} details the novel design of both the local telemetry service from the physical layer of the optical network to distributed databases and the remote telemetry service from distributed databases to the centralized database in the aforementioned proposed unified platform. 

\subsubsection{Semantic Module Designs}

The input contains 12 features derived from 30-s wireless records: aggregate 5G, Wi-Fi, and LiFi traffic; total traffic; mean Wi-Fi and LiFi signal levels; mean Wi-Fi inactivity; a five-record rolling traffic fluctuation; three RAT-activity indicators; and a combined signal-quality score. RAT activity is defined relative to the 25th percentile of training traffic. All min--max and standard scalers are fitted on the training period only. Four task-oriented targets are then formed: load $L$, access diversity $D$, link stability $S$, and congestion risk $C$. With $q$ denoting combined signal quality, $f$ normalised traffic fluctuation, and $i$ normalised inactivity,
\begin{align}
D &= (a_{5G}+a_{WiFi}+a_{LiFi})/3,\\
S &= \left[0.5q+0.3(1-f)+0.2(1-i)\right]_{[0,1]},\\
C &= \sigma(2L+1.5i+f-1.5S-0.8D),
\end{align}
where $[\cdot]_{[0,1]}$ denotes clipping and $\sigma(\cdot)$ is the logistic function. These variables were selected to cover traffic intensity, temporal stability, multi-access availability, and a composite risk indicator using fields present across the wireless testbed exports. A confidence value $1-f$ accompanies each transmitted representation. Fig.~\ref{fig:semantic modules.png} summarises the three designs.

V1 transmits eight deterministic indicators: $L$, $S$, $D$, $C$, rolling-mean traffic, traffic fluctuation, signal quality, and inactivity risk. V2 uses a 12--64--32--8 encoder and a symmetric decoder to reconstruct $L$, $S$, $D$, and $C$. V3 uses a 12--32--16--3 encoder and a symmetric decoder. Its primary setting injects zero-mean Gaussian noise with $\sigma=0.05$ at the encoder input during training. The encoder--decoders use mini-batch Adam, batch size 32, at most 200 epochs, and early stopping with patience 12 on validation loss. For downstream utility, the four decoded targets plus confidence are fused with optical features; the privacy attacker receives the latent code plus confidence. The source, exact seeds, preprocessing parameters, and per-seed outputs are released with the experiment scripts.

\begin{figure}[h]
    \centering
    \includegraphics[width=\linewidth]{Figures/semantic_case_V1_V2_V3_principle_comparison.png}
    \captionsetup{justification=centering}
    \caption{Structures of three semantic-module designs.}
    \label{fig:semantic modules.png}
\end{figure}

The optical and wireless campaigns do not overlap in time. We therefore construct an explicitly labelled interoperability benchmark by mapping the ordered 20,960 optical records to the ordered 2,849 wireless records at uniformly spaced relative positions; no timestamp equality is asserted. This benchmark tests schema compatibility and representation utility, not contemporaneous end-to-end behaviour. After one-step target shifting, 2,848 samples remain. Optical risk is defined using the training-period 15th percentile of Q-factor or 85th percentile of BER. Wireless risk is defined as $C>0.65$, $S<0.35$, or $D<0.34$. Binary service risk is their logical OR; four degradation classes denote neither, wireless only, optical only, and both. These are rule-derived benchmark labels rather than field-observed service failures.


\subsubsection{Testing Results}

Samples are partitioned chronologically into 60\% training, 20\% validation, and 20\% test segments, with a ten-record purge gap before each boundary to prevent five-record rolling features crossing partitions. This yields 1,698/560/570 evaluated samples. Every threshold, scaler, encoder, and downstream classifier is fitted after the split using training data only. Utility is measured with a 300-tree class-balanced random forest over ten seeds (3, 7, 11, 19, 23, 31, 42, 53, 71, and 97). We report Accuracy, Balanced Accuracy, Macro-F1, AUROC, and AUPRC; Table~\ref{tab:semantic_module_results} shows the primary Macro-F1 results and 95\% confidence intervals.

Privacy is operationalised as attribute inference from the transmitted representation. Dominant CPE and dominant RAT are defined by the largest aggregate traffic contribution in each record. Four attackers are evaluated: class-balanced logistic regression, a 300-tree random forest, a two-layer MLP (32 and 16 units), and histogram gradient boosting. Raw telemetry, PCA, random projection, majority, and stratified-random baselines are included. Because the test set contains 559 Wi-Fi and only 11 LiFi records (and no 5G record), RAT accuracy is dominated by the 0.981 majority baseline and is retained only as a diagnostic. CPE Macro-F1 is the primary privacy metric.

\begin{figure*}[t]
    \centering
    \includegraphics[width=0.98\linewidth]{Figures/semantic_module_revision_results.png}
    \captionsetup{justification=centering}
    \vspace{-0.1cm}
    \caption{Leakage-free temporal evaluation. (a) Binary service-risk utility; error bars are 95\% confidence intervals over ten seeds. (b) Utility versus the strongest observed CPE attacker among four attacker families. Lower attacker Macro-F1 indicates less observed leakage but is not a formal privacy guarantee.}
    \label{fig:semantic results}
    \vspace{-0.2cm}
\end{figure*}

V3 reaches binary Macro-F1 $0.6457\pm0.0054$, similar to V1 ($0.6455\pm0.0040$) and above raw wireless plus optical features ($0.6329\pm0.0044$). The overlapping intervals do not support a claim that V3 is superior to V1. For the four-class task, raw features perform best. Moreover, the test distribution is 436/115/13/6, so the four-class result is exploratory. Removing V3 training noise slightly increases binary Macro-F1 from 0.6457 to 0.6473; increasing noise to $\sigma=0.10$ lowers the strongest CPE-attack Macro-F1 to 0.2319. Thus, noise reduces observed leakage but does not improve utility in this dataset.

\begin{table*}[t]
\caption{Temporal-holdout utility and CPE-attribute leakage (mean $\pm$ 95\% confidence interval over ten seeds).}
\label{tab:semantic_module_results}
\centering
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lccc}
\hline
Representation & Binary service-risk Macro-F1 & Four-class Macro-F1 & Strongest CPE-attack Macro-F1$\downarrow$ \\
\hline
Majority baseline & $0.4334\pm0$ & $0.2167\pm0$ & $0.1235\pm0$ \\
Stratified-random baseline & $0.5046\pm0.0147$ & $0.2427\pm0.0101$ & $0.2530\pm0.0095$ \\
Raw wireless + optical & $0.6329\pm0.0044$ & $\mathbf{0.4161\pm0.0051}$ & $0.2757\pm0$ \\
V1 + optical & $0.6455\pm0.0040$ & $0.3823\pm0.0033$ & $0.2481\pm0$ \\
V2 + optical & $0.6374\pm0.0044$ & $0.3958\pm0.0175$ & $0.2469\pm0.0102$ \\
V3 + optical ($\sigma=0.05$) & $0.6457\pm0.0054$ & $0.3955\pm0.0185$ & $0.2382\pm0.0113$ \\
PCA3 + optical & $0.5872\pm0.0081$ & $0.3829\pm0.0045$ & $\mathbf{0.2174\pm0.0059}$ \\
\hline
\end{tabular}
\end{table*}


V3 therefore offers a useful operating point: it retains V1-level binary utility with moderately lower observed CPE leakage. It is not universally optimal---PCA3 leaks less but loses more utility, and raw features perform best on the imbalanced four-class task. We consequently describe V3 as privacy-aware and report the complete trade-off rather than claiming privacy preservation or statistical superiority.



\subsection{Practical Dataset Description}

Under the telemetry architecture, we openly provide datasets from separate optical-transport, fibre-sensing, and wireless-access campaigns~\cite{Shen_dataset_2025}. Table~\ref{tab:dataset_summary} summarises their scope and machine-readable files. The campaigns have different dates, sampling rates, and service contexts; ``cross-domain'' therefore refers to common publication and representation rather than native sample-level alignment. The datasets and associated metadata released in the practical-data and revision repositories are licensed under Creative Commons Attribution--NonCommercial 4.0 International (CC BY-NC 4.0), which permits sharing and adaptation with attribution for non-commercial purposes. We therefore describe these resources as openly available. Software and identified third-party material retain their own terms. A Croissant~1.0-compatible JSON-LD record documents the released data, access locations, sizes, checksums, and source-defined units. The final synthetic wireless bundle is available under \texttt{W4C\_THREE\_RAT\_SWEEP/}~\cite{Shen_dataset_2026}.

\begin{table*}[t]
\caption{Summary of the practical, constructed, and synthetic evaluation datasets used in this paper. Sizes refer to the principal processed files used in the revision experiments; raw sensing traces are distributed as multiple files.}
\label{tab:dataset_summary}
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabularx}{\textwidth}{p{2.4cm}p{3.0cm}X p{2.5cm}p{2.1cm}}
\hline
Dataset & Records/sampling & Principal fields or labels & Format/processed size & Access/reuse terms \\
\hline
339-km NDFF monitoring (2023--24) & 1,373 processed records from 26 Dec. 2023 to 26 Jan. 2024; 10-s raw acquisition resampled to 15 min & Four Voyager BER channels and four derived Q-factors with timestamps & CSV and scripts; 0.15 MB & CC BY-NC 4.0 \\
339-km NDFF Voyager (2025--26) & 20,960 valid raw records from 22 Dec. 2025 to 24 Jan. 2026; mostly 70--72-s spacing with acquisition gaps & Eight channel BER ratios and real timestamps; derived Q-factor used in semantic experiments & Raw CSV; 1.79 MB & CC BY-NC 4.0 \\
986-km NDFF QoT & Two links and ROADM nodes; record count and time coverage not available in the current release & 12 channel states and Q-factors at three nodes; units follow the cited source & External CSV source cited in YangOFC2023; file size not available here & External reference; not in current release \\
Urban fibre sensing & Four-week campaign over 7.776-km and 8-km links; exact record count not available in the current release; polarimeter sampling at 97.656~kSa/s & BER, Q-factor, OSNR, received power, $S_1$--$S_3$, oscilloscope time/voltage; field units follow the source & MAT and CSV; file sizes not available here & External reference; not in current release \\
Wireless access & 2,849 records over 24 h (nominal 30-s telemetry); 46 columns & CPE traffic, 5G temperature/traffic/power, Wi-Fi rate/inactivity/signal, LiFi rate/signal; raw units not declared in source & CSV; merged CSV 0.79 MB & CC BY-NC 4.0 \\
Synthetic GNPy optical DT & 8,192 executions; 6,144 unique configurations (6,120 valid and 24 no-signal); 96-case replay & Four paths, two modulation/spectrum settings, 256 binary occupancy patterns, three launch powers; GSNR, status, warnings, and replay differences & CSV, JSON, and JSONL; principal 6,144-row table 10.09 MB & CC BY-NC 4.0 \\
Synthetic three-RAT wireless DT & 48 configurations; 432 application-flow records (144 each for NR, Wi-Fi~802.11ax, and LiFi); 10-s runs & Per-flow KPIs (Mbps, ms, packets, bytes, ratios) and LiFi optical-state variables (m, rad, W, A, ratios); loads 2/5/10/20~Mbps, speeds 0.5/3~m/s, FOV 40$^\circ$/70$^\circ$, seeds 1--3 & ns-3.44/5G-LENA; CSV, XML, JSON; public three-RAT bundle & CC BY-NC 4.0 \\
Constructed fusion benchmark & 2,848 one-step prediction samples; chronological split with 20 purged boundary records & Current/target source-row indices, deterministic wireless semantics, optical statistics, training-only thresholds, and rule-derived labels & CSV; 1.60 MB & CC BY-NC 4.0 \\
\hline
\end{tabularx}
\vspace{2pt}
\footnotesize The final synthetic wireless fields and units are listed in the public synthetic-wireless data dictionary. For source files not present in the current repository, record counts, file sizes, and field units are not inferred from citations or filenames.
\end{table*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\linewidth]{Figures/setup.png}
    \captionsetup{justification=centering}
    \vspace{-0.1cm}
    \caption{Experimental setups for practical dataset collection: (a) optical transmission performance monitoring over the NDFF, (b) bidirectional fibre sensing over deployed urban fibre links, and (c) multi-access wireless network testbed.}
    \label{fig:setup.png}
    \vspace{-0.3cm}
\end{figure*}

\subsubsection{One-Month Performance Monitoring Data from Optical Transport Network over NDFF}


Figure~\ref{fig:setup.png}(a) presents the experimental setup over part of the UK National Dark Fibre Facility (NDFF), including transponders, erbium-doped fibre amplifiers, wavelength-selective switches, and optical switches. Seven equalised 200-GHz-spaced PM-16QAM/QPSK signals were generated by Facebook Voyager, ADVA FSP~3000 TeraFlex, and Ciena transponders. Voyager and ADVA support 16QAM and QPSK, whereas the Ciena device supports QPSK. The signals traversed the 339-km loop from Bristol through Bradley Stoke, Froxfield, Reading, and Power Gate, with carrier frequencies from 193.25 to 194.45~THz. The launch power was fixed at $-5$~dBm as the operating point used for this campaign; no optimality claim is made. A cloud configuration and monitoring database stored the field-trial measurements.

The public repository distinguishes two measurement campaigns on this same 339-km loop. The earlier release contains 1,373 processed records collected from 26th December 2023 to 26th January 2024. Its 10-s raw BER measurements were resampled to 15-minute intervals, and four Voyager-channel Q-factors were calculated using $Q = \sqrt{2}\,erfcinv(2\,BER)$. This preprocessing reduces short-term measurement noise and aligns the sampling interval with that of the Microsoft open-source dataset.

A separate Christmas 2025 campaign, used as the optical source for the constructed semantic-fusion benchmark in Section~IV-B, contains 20,960 valid eight-channel BER records from 22nd December 2025 16:38:43 to 24th January 2026 16:10:55. These are real acquisition timestamps retained without shifting or anonymisation. The raw CSV preserves 38 embedded headers from 39 concatenated acquisition segments; the released preprocessing removes those header rows before timestamp and BER parsing. Consecutive valid records are mostly 70--72~s apart, with larger acquisition gaps, and this file was not resampled to 15-minute intervals. Its unchanged source file and SHA-256 checksum are published with the acquisition note~\cite{Shen_dataset_2025}. Because the two campaigns differ in date, channel count, and sampling, they are reported as separate products rather than as stages of one transformation.

We also release a complementary dataset from a previous 986-km NDFF field trial~\cite{YangOFC2023} comprising two fibre links and ROADM nodes, where multi-channel Q-factor measurements were collected for end-to-end QoT prediction. Together, the 339-km and 986-km datasets provide representative short- and long-link field-trial scenarios for QoT estimation, short-term performance forecasting and anomaly detection.


\vspace{-0.2cm}
\subsubsection{Four-Week Sensing Data from BDFI and M Shed}

Figure~\ref{fig:setup.png} (b) illustrates the experimental setup for long-term fibre sensing based on bidirectional transmission. The experiment was conducted over two deployed urban fibre links in Bristol: a 7.776-km link connecting our lab in Merchant Ventures Building (MVB) to Bristol Digital Futures Institute (BDFI), and an 8-km link connecting MVB to M Shed Museum. Along the MVB-BDFI route, the fibre passes beneath multiple railway tracks near Bristol Temple Meads Station at the 3.2-km point, where it laterally traverses beneath a railway bridge. In contrast, the MVB-M Shed route does not pass beneath railway tracks.

An ADVA FSP 3000 Teraflex transponder generated two 32-GBaud PM-QPSK optical signals, which were launched into the fibre links through optical circulators to enable counter-propagating transmission. The endpoint Circulators were interconnected through the live field fibre, with the forward and backward channels centred at 193.4 THz and 191.3 THz respectively. At each endpoint, the received optical signal was split using an optical coupler: 90\% of the optical power was directed to the coherent receiver, while the remaining 10\% passed through a polariser and was delivered to a polarimeter and a real-time oscilloscope equipped with a photodetector. This configuration enables simultaneous communication monitoring and fibre sensing. The polariser projects the received state of polarisation (RSOP) onto a fixed eigenstate, allowing environmental disturbances along the fibre to be captured through the measured polarisation dynamics. For vibration localisation, a common trigger from an arbitrary waveform generator was used to synchronise both the polarimeter and oscilloscope.

The dataset spans four weeks and is provided in MAT and CSV formats. It contains communication KPIs, including BER, Q-factor, and OSNR, and polarisation telemetry (Stokes parameters $S_1$, $S_2$, and $S_3$). These measurements were continuously collected by the ISIC end-to-end monitoring platform. Because the data originate from deployed urban fibres rather than laboratory spools, they capture environmental and operational variation. The dataset supports studies of SOP evolution, vibration detection and localisation, communication degradation under physical disturbance, ML-based fibre sensing, and communication--sensing integration.


\subsubsection{24-Hour Status and Performance Data from a Wireless Access Network Testbed}

The wireless dataset was collected from a wireless access network testbed originally developed in the 5G-CLARITY project and further extended in the REASON project. These radio signals and telemetry records originate entirely within that wireless testbed; they are not converted from, transported as, or synchronised to signals on either optical field link. The optical interconnection shown in Figs.~\ref{fig:proposed architecture.png} and~\ref{fig:cross-domain telemetry.png} is a system-level architecture, not the signal source for this campaign. As shown in Fig.~\ref{fig:setup.png}(c), the testbed integrates 5G New Radio, Wi-Fi~6, and LiFi. Four fixed CPEs were deployed: CPE1--CPE3 were dual-homed to 5G and Wi-Fi, while CPE4 was tri-homed to 5G, Wi-Fi, and LiFi. All CPEs used MPTCP~v1 in fullmesh mode with the default BLEST scheduler. During collection, CPE1--CPE3 generated randomised iPerf traffic towards CPE4.

The dataset provides 24 hours of time-aligned wireless telemetry, covering per-interface CPE traffic, 5G cell metrics, LiFi cell metrics, and Wi-Fi cell metrics. Specifically, it includes per-interface traffic distribution, 5G temperature, throughput and power, LiFi transmit/receive rates and signal power, and Wi-Fi bitrates, inactivity time and average signal levels. This dataset offers a comprehensive view of heterogeneous wireless performance and supports research on AI-driven multi-access optimisation, multi-connectivity control, MPTCP scheduling, and cross-technology coordination in 5G and beyond networks. By capturing the complementary characteristics of 5G, Wi-Fi, and LiFi in a unified testbed, it also provides a basis for wireless DTs, semantic telemetry abstraction, and cross-domain optimisation with optical datasets.




% \begin{figure*}[t]
%    \centering
%    \begin{subfigure}[b]{0.47\linewidth}
%        \centering
%        \includegraphics[scale=0.45]{Figures/topology1.eps}
%        \captionsetup{justification=centering}
%        \caption{}
%        \label{subfig:1}
%    \end{subfigure}
%    \hfill
%    \begin{subfigure}[b]{0.42\linewidth}
%        \centering
%        \includegraphics[scale=0.42]{Figures/schedule2.eps}
%        \captionsetup{justification=centering}
%        \caption{}
%        \label{subfig:2}
%    \end{subfigure}
%    \captionsetup{justification=centering}
%    \caption{(a) Experimental setup for two use cases, (b) Sequence diagram of the proposed AI engine platform}
%    \label{fig:figure8}
% \end{figure*}

\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth,trim=740 420 840 460,clip]{Figures/architecture_LLM_revision.png}
    \captionsetup{justification=centering}
    \caption{Evaluated data-generation sequence. Fig.~\ref{fig:proposed architecture.png} provides the framework scope, whereas this figure shows the shared task state, role-specific LLM invocations, simulator interface, and retained artefacts.}
    \label{fig:architecture_LLM}
    \vspace{-0.3cm}
\end{figure*}

\section{LLM-Driven Multi-Agent Digital Twins for Automated Cross-Domain Data Generation}
\label{sec:LLM}

Practical telemetry cannot cover every topology, loading condition, or rare event. This section presents an LLM-driven multi-agent DT workflow as a feasibility demonstration for generating complementary synthetic data.



\subsection{Proposed LLM-Driven Multi-Agent Digital Twin Framework}

Figure~\ref{fig:architecture_LLM} shows the operational definition of ``multi-agent'' used here: five schema-defined functional roles coordinated through orchestrator-owned shared state. Planner, Scenario Expansion, and Reflection invoke an LLM; Results Analysis and Report are deterministic. Topology and equipment files, KPI definitions, simulator wrappers, and stored execution results provide the roles' knowledge and tools. The orchestrator passes a natural-language request through planning, scenario expansion, DT execution, analysis, optional refinement, and reporting.

The reported workflow invokes GNPy and ns-3 as DT engines and retains structured requests, configurations, results, and logs. It demonstrates that the role sequence can produce auditable simulator artefacts; it does not establish that every role requires an LLM or that the decomposition is more efficient than a conventional script.

\subsection{Evaluation Protocol and Reproducibility Scope}

We compare the GNPy demonstration with an independent deterministic enumerator over four destinations, two modulation/slot-width settings, 256 occupancy patterns, and three launch powers. Configuration keys exclude iteration and scenario identifiers, allowing repeated executions to be separated from the 6,144 unique cases. The script enumerates the Cartesian product only; it does not perform intent parsing or optical propagation.

For reproducibility, a separately labelled instrumented run used \texttt{gpt-4o-mini-2024-07-18}, temperature 0.2, a 90-s timeout, at most three attempts per call, and strict mode. Planner, Scenario Expansion, and Reflection were each called twice. All six API calls succeeded on their first attempt, using 108,308 tokens, 26.22~s summed API latency, and an estimated \$0.01727 in text-token cost. Complete prompts, responses, logs, recovery history, and source/input hashes are released at commit \texttt{82914127b70acf3b220bc09a56a179cb7abd1252}~\cite{JOCN_revision_code_2026}. Manual recovery was required during simulator execution; accordingly, these measurements describe one feasibility run rather than unattended reliability or agent superiority.


\begin{table}[t]
\caption{Instrumented OpenAI feasibility run.}
\label{tab:llm_trace}
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\columnwidth}{p{2.15cm}X}
\hline
Item & Observed value \\
\hline
Model/settings & \texttt{gpt-4o-mini-2024-07-18}; temperature 0.2; timeout 90~s; maximum three attempts; strict mode \\
Role calls & Planner 2; Scenario Expansion 2; Reflection 2 \\
API outcomes & 6/6 successful first attempts; 0 retries; 0 API failures \\
Token usage & 106,041 prompt; 2,267 completion; 108,308 total \\
API latency & 26.22~s summed; 2.88--7.61~s per call \\
Estimated cost & \$0.01727 using the execution-date pricing snapshot \\
Retained evidence & Rendered requests/responses, call log, recovery record, and source/input hashes \\
\hline
\end{tabularx}
\end{table}

\begin{table}[t]
\caption{Configuration-set baseline for the corrected GNPy demonstration.}
\label{tab:llm_baseline}
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrr}
\hline
Method & Executions & Unique & Duplicate rate \\
\hline
Deterministic enumerator & 0 & 6,144 & 0 \\
LLM-orchestrated history & 8,192 & 6,144 & 25\% \\
\hline
\end{tabular}
\vspace{2pt}

\footnotesize Both sets have precision and recall 1.0 against the independently specified 6,144-key set. The deterministic baseline performs configuration enumeration only, uses no LLM/API calls, and is not an end-to-end runtime comparison.
\end{table}

\subsection{Use Cases and Demonstration Results}

We demonstrate the workflow with GNPy for optical transport and ns-3 for wireless access. The final synthetic wireless source and results are released under \texttt{W4C\_THREE\_RAT\_SWEEP/}~\cite{Shen_dataset_2026}; the upstream \texttt{NS3\_Wireless/} directory is an earlier baseline.


\begin{figure*}[t]
    \centering
    \includegraphics[width=0.98\linewidth]{Figures/usecase1_corrected.pdf}
    \captionsetup{justification=centering}
    \caption{GNPy feasibility demonstration over the NDFF topology. (a) Configuration enumeration and execution-history counts; (b) GSNR distributions for the two modulation/slot-width settings at $-5.5$~dBm; (c) route-dependent mean GSNR change across the launch-power sweep; and (d) QPSK mean GSNR versus active-channel count. Numerical panels use 6,120 valid unique configurations; 24 all-off cases are excluded from metric summaries. GSNR is measured over the signal bandwidth.}
    \label{fig:usecase1}
    \vspace{-0.3cm}
\end{figure*}

\subsubsection{GNPy-Based Network-Level QoT Data Generation}

In this use case, the workflow generates an optical QoT artefact based on the NDFF topology. The request covers paths from Bristol, Voyager transmitters, eight WDM slots, and QPSK and 16QAM configurations. The Planner forms the study specification, and Scenario Expansion constructs the candidate paths, binary channel-occupancy patterns, and launch-power settings for GNPy execution.

As illustrated in Fig.~\ref{fig:usecase1}(a), four paths, two joint modulation/spectrum configurations, and $2^8=256$ occupancy patterns produce 2,048 cases per launch power. The first pass contains 2,048 records at $-5.5$~dBm; the refined pass contains 6,144 records at $-6.5$, $-6.0$, and $-5.5$~dBm. The combined 8,192 execution records reduce to 6,144 unique configurations because the first-pass keys recur in the refined pass. Of the unique configurations, 6,120 return numerical results and 24 are expected all-off, no-signal cases. The observed set has no missing or additional keys relative to the deterministic enumerator.

Figure~\ref{fig:usecase1}(b)--(d) uses the unique refined-pass records. At $-5.5$~dBm, mean QPSK GSNR decreases from 26.34 to 22.57~dB on Bristol--Bradley Stoke and from 9.52 to 3.80~dB on Bristol--Powergate as active-channel count rises from one to eight. Launch-power sensitivity is route dependent: over the tested range, the Bradley Stoke mean decreases whereas the Powergate mean increases slightly. These three operating points do not establish a globally optimal launch power.


For numerical reproducibility, we independently replayed 96 cases stratified by path, joint modulation/slot-width setting, power, and channel load. A pinned GNPy v2.12 compatibility environment completed all 96 cases and reproduced the archived GSNR values within 0.00050~dB. A schema-corrected GNPy v2.14.2 environment also completed all cases but differed from the archive by 0.628~dB on average and 1.435~dB at most. The version sensitivity is larger on multi-span routes. Configuration warnings persist, so successful execution establishes numerical reproducibility under the stated environments rather than warning-free physical calibration.

\begin{table}[t]
\caption{GNPy replay over 96 stratified cases.}
\label{tab:gnpy_replay}
\centering
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrr}
\hline
Environment & Success & Mean $|\Delta\mathrm{GSNR}|$ & Max $|\Delta\mathrm{GSNR}|$ \\
\hline
v2.12 compatibility & 96/96 & 0.00015 dB & 0.00050 dB \\
v2.14.2 schema-fixed & 96/96 & 0.628 dB & 1.435 dB \\
\hline
\end{tabular}
\end{table}

QPSK and 16QAM use 37.5- and 50-GHz slots, respectively; their observed differences therefore reflect a joint modulation/spectral-spacing setting rather than a modulation-only effect.

% Figure~\ref{fig:usecase1}(b) presents the GSNR distributions across the four investigated NDFF paths under QPSK and 16QAM configurations at a launch power of $-5.5$ dBm. The Bristol–Bradley Stoke path achieves substantially higher GSNR values than the longer paths, whereas the distributions for Froxfield, Reading, and Powergate are progressively concentrated in lower GSNR regions. 


% This result demonstrates that the generated dataset captures route-dependent QoT characteristics associated with different transmission distances and network configurations. In addition, the figure shows modulation-dependent GSNR variations under the transceiver and spectrum configurations specified in the digital twin, thereby providing diversified samples for downstream QoT-related ML tasks.

% The role of agent-driven iterative refinement is further illustrated in Fig.~\ref{fig:usecase1}(c). Rather than limiting data generation to a single predefined operating point, the Reflection Agent extends the initial execution space by introducing additional launch-power settings according to the evaluated GSNR results. For the Bristol–Bradley Stoke path, increasing the launch power from (-5.5) to (-4.5) dBm reduces the average GSNR by approximately 1.59 dB. In contrast, the longer paths exhibit considerably smaller variations within the investigated power range; in particular, the Powergate path achieves a slight GSNR improvement as the launch power increases. These results indicate that the influence of launch-power adjustment is route dependent, motivating iterative scenario expansion rather than fixed-parameter dataset generation.

% Figure~\ref{fig:usecase1}(d) further examines the impact of channel loading using QPSK transmission at (-5.5) dBm. For all four NDFF paths, the mean GSNR decreases as the number of active WDM channels increases. Specifically, the mean GSNR of the Bristol–Bradley Stoke path decreases from 26.35 dB with one active channel to 22.59 dB with all eight channels active. Similarly, the corresponding GSNR values decrease from 10.21 to 5.48 dB for Froxfield, from 10.11 to 5.39 dB for Reading, and from 9.95 to 5.24 dB for Powergate. This consistent degradation trend demonstrates that the generated dataset captures channel-loading-dependent performance variation, which is important for QoT prediction and network optimisation under dynamically changing spectrum occupancy.

Overall, the use case demonstrates conversion of a high-level request into structured simulator artefacts and a reflection-proposed refined sweep. The deterministic baseline shows that an LLM is unnecessary for exhaustive enumeration of this fixed space. Larger topologies, repeated requests, and calibrated equipment models remain future work.


\subsubsection{ns-3-Based Multi-Access Network Performance Data Generation}
\label{sec:ns3_wireless}

The LLM-driven workflow constructs and executes a bounded three-access synthetic wireless scenario at the same technology-class level as the practical multi-access testbed: 5G NR, Wi-Fi~6/IEEE~802.11ax, and LiFi. The generated dataset is separate from the measured wireless telemetry. It demonstrates configurable synthetic data generation and workflow interoperability, not calibrated emulation of the physical testbed and not validation of measured wireless values.

Each scenario contains three common mobile CPEs, each carrying one interface for each access branch. The NR branch uses CTTC 5G-LENA version 4.0.y on ns-3.44 with one gNB and three UEs, a 3.5-GHz carrier, 40-MHz bandwidth, numerology 1, 30-dBm gNB transmit power, and the 3GPP InH-OfficeOpen channel. The Wi-Fi branch uses native \texttt{WIFI\_STANDARD\_80211ax} with one AP and three stations, a 5-GHz 20-MHz channel, fixed 16-dBm transmit power, and an explicit constant-rate \texttt{HeMcs0} configuration. CPE mobility is RandomWalk2d within $50\times50$~m bounds at 0.5 or 3~m/s. All branches use UDP downlink traffic with 1,024-byte packets, a common offered load of 2, 5, 10, or 20~Mbps per application flow, a 1.0--9.5~s application window, and a 10-s simulation stop.

The LiFi branch is an equation-driven LOS LiFi/OWC link model. It computes Lambertian emission, transmitter/receiver geometry, receiver FOV, received optical power, photodetector responsivity, shot and thermal noise, electrical SNR, OOK BER, and packet-error probability. A stock ns-3 point-to-point packet path is used as the IP/UDP access abstraction, while packet corruption is driven by the optical link calculation. This is not a complete standardised LiFi PHY/MAC and its explicit parameters are uncalibrated to the practical testbed.

For half-power semi-angle $\Phi_{\mathrm{half}}$, detector area $A$, distance $d$, irradiance angle $\phi$, incidence angle $\psi$, filter gain $T_s$, and concentrator gain $g(\psi)$, the Lambertian order is $m=-\ln(2)/\ln(\cos\Phi_{\mathrm{half}})$ and
\begin{equation}
H_{\mathrm{LOS}}=\frac{(m+1)A}{2\pi d^2}\cos^m(\phi)T_sg(\psi)\cos(\psi),
\end{equation}
when the irradiance geometry is valid and $\psi$ is within the receiver FOV; otherwise $H_{\mathrm{LOS}}=0$. The concentrator is $g(\psi)=n^2/\sin^2(\Psi_c)$ within the FOV, and received optical power is $P_r=P_tH_{\mathrm{LOS}}$. The receiver current and the shot/thermal-noise terms determine SNR, which is mapped to $\mathrm{BER}=Q(\sqrt{\mathrm{SNR}}/2)$ and $\mathrm{PER}=1-(1-\mathrm{BER})^{8L}$. These equations follow the standard LOS optical-wireless link-budget formulation~\cite{Kahn1999,Komine2004} and are implemented in the source archived under \texttt{W4C\_THREE\_RAT\_SWEEP/ns3/}~\cite{Shen_dataset_2026}.

Four offered loads, two mobility speeds, two LiFi receiver FOVs (40$^\circ$ and 70$^\circ$), and seeds 1, 2, and 3 yield $4\times2\times2\times3=48$ run-level configurations. The 40$^\circ$ setting was selected by a preflight over 30$^\circ$, 40$^\circ$, 50$^\circ$, and 70$^\circ$; it produced measurable out-of-FOV states without near-total outage under the declared geometry. Each final configuration produces nine application flows (three branches $\times$ three CPEs), giving 432 application-flow records. The 624 raw FlowMonitor rows additionally include infrastructure/control flows, which are excluded from application statistics. Outputs comprise FlowMonitor XML, per-flow KPI CSV, scenario/configuration metadata, explicit flow mappings, sampled LiFi optical states, and aggregated CSV files.

\paragraph{KPI Definitions and Interpretation.}
For received bytes $B_{\mathrm{rx}}$, transmitted/received packet counts $N_{\mathrm{tx}}$ and $N_{\mathrm{rx}}$, and FlowMonitor time quantities in seconds, the exported throughput is $8B_{\mathrm{rx}}/[10^6(t_{\mathrm{lastRx}}-t_{\mathrm{firstTx}})]$~Mbps. Mean delay is $10^3\,\mathrm{delaySum}/N_{\mathrm{rx}}$~ms; mean jitter is $10^3\,\mathrm{jitterSum}/(N_{\mathrm{rx}}-1)$~ms for $N_{\mathrm{rx}}>1$. The FlowMonitor-reported packet-loss ratio is $\mathrm{lostPackets}/N_{\mathrm{tx}}$. Because traffic and simulation terminate at finite times, this field is not interpreted as terminal packet loss; 172 of 432 application rows have $N_{\mathrm{tx}}-N_{\mathrm{rx}}\ne\texttt{lostPackets}$.

\paragraph{Validation of the Synthetic Wireless Dataset.}
Successful execution alone does not establish generated-data correctness. We therefore evaluated the final stack at the backend, equation, integrated-flow, repeatability, and full-sweep levels (Table~\ref{tab:ns3_validation}). The NR/5G-LENA and native 802.11ax branches passed build and runtime checks. The independent LiFi mathematical tests passed 9/9 checks, and compiled ns-3 link-probe values agreed with the independent Python reference in 15/15 comparisons across aligned-near, aligned-far, and out-of-FOV geometries. The integrated acceptance run produced three flows per branch and 9/9 mapped application flows.

The final-source configuration was then executed twice with the same ns-3.44 build, seed, run number, workload, mobility, and FOV. The two outputs were byte-identical for the run configuration, scenario metadata, flow mapping, KPI CSV, FlowMonitor XML, and optical-state CSV. Finally, the bounded sweep completed all 48 configurations, with 432/432 application-flow mappings passing and no missing, extra, or duplicate configuration keys. These checks support correctness and reproducibility of the generated synthetic telemetry under the declared assumptions; they do not establish physical-testbed calibration or physical-model fidelity.

\begin{table}[t]
\caption{Validation of the ns-3 synthetic wireless dataset.}
\label{tab:ns3_validation}
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{p{2.0cm}X X}
\hline
Validation & Coverage & Result \\
\hline
Backend qualification & 5G-LENA NR and native 802.11ax branches & Build/runtime PASS in the qualified ns-3.44 environment \\
LiFi equation tests & Independent Python reference; 9 tests & 9/9 PASS \\
Compiled LiFi reference & 3 geometries; 15 values & 15/15 comparisons PASS \\
Integrated three-RAT mapping & 1 final-source acceptance; 9 application flows & 3 NR + 3 Wi-Fi + 3 LiFi; 9/9 mapped \\
Same-seed repeatability & 1 final-source configuration executed twice & Byte-identical KPI, XML, mapping, metadata, and optical-state outputs \\
Full final sweep & 48 configurations; 624 raw / 432 application flows & 48/48 complete; 432/432 mappings pass \\
\hline
\end{tabularx}
\vspace{2pt}
\footnotesize The rows distinguish backend qualification, independent equation/reference checks, integrated flow mapping, fixed-seed repeatability, and bounded-sweep completion under the declared scenario. None is physical-model validation.
\end{table}

Under the adopted simulation configuration, increasing the offered load from 2 to 20~Mbps increased run-level throughput by 18.486~Mbps for NR and 15.726~Mbps for LiFi. For Wi-Fi 802.11ax, the corresponding throughput increase was only 0.151~Mbps, while mean delay increased by 1,653.88~ms and the FlowMonitor-reported packet-loss ratio increased by 0.701. These results indicate a configuration-specific queueing response in the Wi-Fi branch and load-responsive throughput in the NR and LiFi branches. Increasing mobility from 0.5 to 3~m/s changed throughput by 0~Mbps for NR, 0.00018~Mbps for Wi-Fi 802.11ax, and $-2.572$~Mbps for LiFi under the selected geometry and channel models. Narrowing the LiFi FOV from 70$^\circ$ to 40$^\circ$ reduced the in-FOV fraction by 0.299 and the received-packet fraction by 0.299 in matched run-level comparisons. Because the ideal concentrator gain also changes with FOV, aggregate $H_{\mathrm{LOS}}$ and SNR are not asserted to decrease monotonically. Branch comparisons depend on topology, channel/helper assumptions, link rates, delays, offered load, mobility, and LiFi modelling choices; they are not universal technology rankings.

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth,trim=0 0 0 28,clip]{Figures/w4c_throughput_vs_load.pdf}
    \caption{Synthetic wireless throughput versus offered load. For each load and branch, the three CPE flows are averaged within each run, nuisance speed/FOV conditions are averaged within each seed, and error bars show the standard deviation across the three seeds.}
    \label{fig:w4c_throughput}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth,trim=0 0 0 28,clip]{Figures/w4c_lifi_fov_response.pdf}
    \caption{Synthetic LiFi/OWC response to receiver FOV. The in-FOV fraction is computed from sampled optical states; the received-packet fraction is computed from exported LiFi flow counters. Bars show mean $\pm$ SD across three seeds after CPE and nuisance-condition averaging.}
    \label{fig:w4c_lifi_fov}
\end{figure}

\paragraph{Wireless Scope and Limitations.} The final synthetic backend uses 5G NR via CTTC 5G-LENA, Wi-Fi 802.11ax, and an equation-driven LOS LiFi/OWC model. The LiFi model uses a stock packet-access abstraction and is not calibrated to the practical testbed. Technology comparisons are therefore specific to the declared configuration. The validation establishes implementation consistency and reproducibility of the bounded synthetic telemetry rather than physical-model fidelity.

\section{Conclusions}
\begin{comment}
In this study, we presented an open-source cross-domain dataset framework for optical transport and wireless access networks, integrating practical network measurements, semantic data abstraction, and LLM-driven multi-agent DT-based data generation. The released practical datasets include long-term optical transmission performance over the NDFF, fibre-sensing measurements over deployed urban fibre links, and multi-access wireless network performance involving 5G, Wi-Fi, and LiFi. In addition, the proposed privacy-aware semantic module provides compact and task-relevant wireless representations for cross-domain fusion while achieving a favourable privacy–utility trade-off.

To complement practical measurements, the proposed LLM-driven multi-agent DT framework enables automated data generation and iterative refinement under configurable network conditions. In the GNPy-based optical QoT use case, the framework generated 8,160 valid samples from 8,192 executed scenarios, covering multiple NDFF paths, modulation formats, channel-occupancy patterns, and launch-power settings. In the ns-3-based wireless use case, it generated multi-access performance data across Wi-Fi, LTE, and LiFi-surrogate branches, including throughput, packet loss, delay, and jitter KPIs. Overall, the released datasets, together with the proposed telemetry and data-generation framework, provide an open and extensible foundation for ML training, evaluation, and updating in future cross-domain network intelligence. Future work will extend DT-based data generation to more diverse wireless access scenarios and investigate joint learning from practical and synthetic cross-domain datasets.

% Future work will focus on larger-scale optical–wireless co-simulation, more realistic wireless PHY/MAC modelling, and joint learning from practical and generated datasets.

\end{comment}

We presented an openly available dataset framework spanning independently acquired optical-transport, fibre-sensing, and wireless-access campaigns together with constructed and synthetic evaluation data. Cross-domain denotes common publication, metadata, representation, and task-level fusion; the practical campaigns are not simultaneous end-to-end measurements.

On the constructed benchmark, the three-dimensional semantic representation preserves the binary-task utility of the hand-crafted representation while moderately reducing observed CPE-attribute leakage. This is an empirical privacy--utility trade-off rather than a formal privacy guarantee. The LLM-driven multi-agent DT workflow demonstrates auditable interaction with GNPy and ns-3, while the deterministic baseline shows that exhaustive enumeration of the fixed optical space does not require an LLM. Versioned replay establishes numerical reproducibility boundaries but also exposes simulator sensitivity and residual configuration warnings. Future work will focus on calibrated physical models, contemporaneous cross-domain measurements, and repeated workflow evaluation on larger scenario spaces.

\section*{Acknowledgement}
This work was partly supported by the European Commission’s Horizon research and innovation program: Allegro project (No. 101092766) and the EU-funded project: ECO-eNET (No. 10113933).

\bibliography{sample.bib}

\end{document}
