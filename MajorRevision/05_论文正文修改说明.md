# 论文正文修改说明

修改文件：`../1-论文正文/JOCN_Telemetry.tex`（实际路径位于与 `JOCN-Dataset` 同级的 `1-论文正文` 目录）。

## 已完成

1. 重写摘要，区分实践数据、构造的融合基准和 DT 生成数据，并加入 E1--E4 的主要结果。
2. 重新组织贡献层级，明确实践数据由 telemetry/semantic abstraction 支撑，生成数据由 LLM-driven DT workflow 支撑。
3. 明确 cross-domain 不表示三个实践数据集同时采集，也不表示光与无线之间存在实测因果关系。
4. 重写语义模块实验：补充原始特征、语义公式、V1/V2/V3 架构、训练参数、规则标签、时序划分、purge gap、10 个随机种子和置信区间。
5. 将原先单一随机森林隐私攻击改为逻辑回归、随机森林、MLP、梯度提升四类攻击器，并加入 raw、PCA、随机投影、多数类和随机基线。
6. 替换旧语义结果表和图，结论改为“V3 是一个有用的 privacy--utility operating point”，不再声称显著优于 V1 或提供隐私保证。
7. 增加实践及构造数据集汇总表，列出记录数/采样、字段、格式、处理后大小和仓库使用条款。
8. 删除无关的 AC/DC 供电细节，将 $-5$ dBm 改为本次采集采用的 operating point，不再称为最优功率，并修复正文拼写错误。
9. 区分架构中 implemented、evaluated、demonstrated 和 prospective 组件；明确 AI engine/controller 闭环未在本文验证，也取消未实测的 scalability 主张。
10. 将五个 agents 定义为 orchestrated LLM-invocation roles，说明共享状态、工具和 orchestrator 的实际职责。
11. 增加确定性配置枚举基线：6,144 个唯一配置、0.0063 s、无 LLM/API 调用；公开 agent artifact 为 8,192 次执行、25% 重复率。
12. 修正 GNPy 第一轮功率为 $-5.5$ dBm，区分 execution records、unique configurations 和 no-signal configurations。
13. 加入 GNPy 警告审计、2.13/2.14 版本兼容性失败、公开 commit、96 个独立复现尝试和调制/槽宽混杂因素。
14. 重写结论，将 LLM 框架限定为 feasibility demonstration，并列出版本容器、完整 agent trace、真正同时采集的跨域 session 等未来工作。
15. 统一主要标题的大小写和 `ns-3` 写法，正文保持英式拼写。
16. 进一步压缩 Section III 和 IV-A 的平台通用描述，明确 Fig. 1 是完整范围图、Fig. 6 是被评估的数据生成序列，且 orchestrator 的右下位置不表示只控制最后两个角色。
17. 明确实践无线信号完全来自无线接入实验台，不由任一光链路转换而来，也未与光学实验进行物理层同步。
18. 从 `figures/Architecture.pptx` 的第 27 页修正 Reflection Agent 的文字对比度，并重新导出 4621×2209 的 Fig. 6。
19. 从 6,144 条去重 GNPy 记录重绘 5846×2526 的 Fig. 7，修正旧图中 execution/unique/valid/no-signal 数量混用的问题。
20. 增加 Croissant 1.0-compatible JSON-LD 元数据，记录 5 个关键处理后数据/实验结果文件的字段、相对路径、字节数和 SHA-256。

## 生成文件

- 修订正文：`F:/BaiduSyncdisk/1-论文/7-Datageneration/1-论文正文/JOCN_Telemetry.tex`
- 可编译预览：`F:/BaiduSyncdisk/1-论文/7-Datageneration/1-论文正文/JOCN_Telemetry_revision.pdf`
- 新结果图：`F:/BaiduSyncdisk/1-论文/7-Datageneration/1-论文正文/Figures/semantic_module_revision_results.png`
- 修订架构图：`F:/BaiduSyncdisk/1-论文/7-Datageneration/1-论文正文/Figures/architecture_LLM_revision.png`
- 修订 GNPy 图：`F:/BaiduSyncdisk/1-论文/7-Datageneration/1-论文正文/Figures/usecase1_revision.png`
- 绘图脚本：`MajorRevision/make_paper_revision_figures.py`
- GNPy 绘图脚本：`MajorRevision/make_gnpy_revision_figure.py`
- Croissant 元数据：`MajorRevision/croissant_metadata.json`
- 逐条处理状态：`MajorRevision/06_审稿意见处理状态.md`

## 仍需作者提供或处理

- 原始 LLM 模型、版本、prompt、tool schema、API 调用、token、成本、延迟、失败及人工干预日志；当前正文诚实标为未记录，并限制为可行性展示。
- 能成功加载公开设备文件的原始 GNPy commit/容器，或修正后的设备配置及重新生成的数据。
- `sample.bib` 中原有的重复 BibTeX key。正文使用现有 `.bbl` 可以编译，但正式清理参考文献库时需要去重。
- 无线/ns-3 新实验仍按作者分工未修改。
- Croissant 文件目前位于 revision artifacts；最终公开仓库提交时需要一并上传，并由作者确认正式 licence 文本或 SPDX licence。
